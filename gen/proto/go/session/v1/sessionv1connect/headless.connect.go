// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: session/v1/headless.proto

package sessionv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/tstapler/stapler-squad/gen/proto/go/session/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HeadlessServiceName is the fully-qualified name of the HeadlessService service.
	HeadlessServiceName = "session.v1.HeadlessService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HeadlessServiceRunHeadlessCallProcedure is the fully-qualified name of the HeadlessService's
	// RunHeadlessCall RPC.
	HeadlessServiceRunHeadlessCallProcedure = "/session.v1.HeadlessService/RunHeadlessCall"
)

// HeadlessServiceClient is a client for the session.v1.HeadlessService service.
type HeadlessServiceClient interface {
	// RunHeadlessCall runs a headless LLM call and streams chunks back to the client.
	RunHeadlessCall(context.Context, *connect.Request[v1.RunHeadlessCallRequest]) (*connect.ServerStreamForClient[v1.RunHeadlessCallResponse], error)
}

// NewHeadlessServiceClient constructs a client for the session.v1.HeadlessService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHeadlessServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HeadlessServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	headlessServiceMethods := v1.File_session_v1_headless_proto.Services().ByName("HeadlessService").Methods()
	return &headlessServiceClient{
		runHeadlessCall: connect.NewClient[v1.RunHeadlessCallRequest, v1.RunHeadlessCallResponse](
			httpClient,
			baseURL+HeadlessServiceRunHeadlessCallProcedure,
			connect.WithSchema(headlessServiceMethods.ByName("RunHeadlessCall")),
			connect.WithClientOptions(opts...),
		),
	}
}

// headlessServiceClient implements HeadlessServiceClient.
type headlessServiceClient struct {
	runHeadlessCall *connect.Client[v1.RunHeadlessCallRequest, v1.RunHeadlessCallResponse]
}

// RunHeadlessCall calls session.v1.HeadlessService.RunHeadlessCall.
func (c *headlessServiceClient) RunHeadlessCall(ctx context.Context, req *connect.Request[v1.RunHeadlessCallRequest]) (*connect.ServerStreamForClient[v1.RunHeadlessCallResponse], error) {
	return c.runHeadlessCall.CallServerStream(ctx, req)
}

// HeadlessServiceHandler is an implementation of the session.v1.HeadlessService service.
type HeadlessServiceHandler interface {
	// RunHeadlessCall runs a headless LLM call and streams chunks back to the client.
	RunHeadlessCall(context.Context, *connect.Request[v1.RunHeadlessCallRequest], *connect.ServerStream[v1.RunHeadlessCallResponse]) error
}

// NewHeadlessServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHeadlessServiceHandler(svc HeadlessServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	headlessServiceMethods := v1.File_session_v1_headless_proto.Services().ByName("HeadlessService").Methods()
	headlessServiceRunHeadlessCallHandler := connect.NewServerStreamHandler(
		HeadlessServiceRunHeadlessCallProcedure,
		svc.RunHeadlessCall,
		connect.WithSchema(headlessServiceMethods.ByName("RunHeadlessCall")),
		connect.WithHandlerOptions(opts...),
	)
	return "/session.v1.HeadlessService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HeadlessServiceRunHeadlessCallProcedure:
			headlessServiceRunHeadlessCallHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHeadlessServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHeadlessServiceHandler struct{}

func (UnimplementedHeadlessServiceHandler) RunHeadlessCall(context.Context, *connect.Request[v1.RunHeadlessCallRequest], *connect.ServerStream[v1.RunHeadlessCallResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("session.v1.HeadlessService.RunHeadlessCall is not implemented"))
}
