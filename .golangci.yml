version: "2"

run:
  go: "1.23"

linters:
  default: fast
  disable:
    - depguard  # Pre-existing issues
    - lll       # Pre-existing issues  
    - mnd       # Pre-existing issues (magic numbers)
    - wsl       # Pre-existing issues (whitespace)
