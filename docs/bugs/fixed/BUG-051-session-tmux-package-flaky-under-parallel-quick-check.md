# BUG-051: `session/tmux` Package Tests Flake Under `make quick-check`'s Parallel Load [SEVERITY: Low]

**Status**: ✅ Fixed (main@dccee742a, 2026-08-04)
**Discovered**: 2026-07-27, while running `make quick-check` as the final AC-7 gate for `insights-cost-pricing-gaps` (backlog bcb20604). Not caused by that project's diff — `session/tmux` was never touched by it (confirmed via `git diff --stat main...HEAD -- session/tmux/`, zero output across the entire change).
**Impact**: `make quick-check` / `make test-coverage` intermittently fails on real tmux server lifecycle tests in `session/tmux`, blocking a clean "all green" run of the standard pre-push gate. Does not affect runtime correctness of the shipped app — only test-suite reliability under this specific `go test` invocation's parallelism/resource pressure.

## Problem Description

Ran `make quick-check` three times in a row against an otherwise-unrelated diff. Each run failed `test-coverage`, but with a **different** failing test each time, always inside `session/tmux`:

1. Run 1 & 2: `TestEnsureServerRunning_NoOp` (`session/tmux/tmux_test.go:273`) — `tmux start-server failed: exit status 1 (output: server exited unexpectedly)`.
2. Run 3: `TestKillOrphanedControlModeClients` (`session/tmux/kill_orphaned_control_mode_clients_test.go:87`) — expected 2 killed orphaned clients, got 0. The *same* run also re-failed `TestEnsureServerRunning_NoOp` with the identical error as runs 1/2.

Each failing test **passes reliably when run in isolation** (`go test ./session/tmux/ -run TestEnsureServerRunning_NoOp -v` → PASS). This points to real tmux-server contention/resource exhaustion when many tests in this package spin up, kill, and restart actual tmux servers concurrently under `go test`'s default parallelism — not a logic bug in the tests or the code under test.

This is a known-fragile area: `git log --oneline -- session/tmux/tmux_test.go` shows a prior dedicated fix (`e476853dc fix(tmux): recover EnsureServerRunning from a transient check-race`) for an adjacent race in the same file, suggesting this class of flake has recurred before and wasn't fully closed.

## Suggested Investigation (not done here — out of scope for the project that surfaced this)

- Check whether `session/tmux`'s tests can be forced to run serially (`t.Parallel()` audit, or `go test -p 1`) without an unacceptable CI time cost.
- Check whether tests that spin up a real tmux server should share a single test-scoped server instance instead of each starting/killing their own, if the current per-test lifecycle is what's contending for the same underlying `tmux` binary/socket.
- Consider whether this sandboxed/CI environment has a tmux-server resource limit (max sessions, socket handles) that's being hit under full parallel load specifically, separate from any local-dev-machine behavior.

## Related

- Not related to `insights-cost-pricing-gaps` (backlog bcb20604) or any of its pricing/insights changes — filed as a fast-follow per this repo's convention of documenting adjacent issues found during unrelated work rather than silently ignoring them (see `ADR-002`/`ADR-003` in `project_plans/insights-cost-pricing-gaps/decisions/` for the same pattern applied to two other findings from that project).

## Recurrence log

- 2026-08-05, while running `make ci`/`make test` as the AC-7 gate for `stapler-squad-fix-idle-reviewer-wedge`: `TestEnsureServerRunning_NoOp` failed identically (`tmux start-server failed: exit status 1 (output: server exited unexpectedly)`) under the full-suite parallel run, passed 5/5 in isolation (`go test ./session/tmux -run TestEnsureServerRunning_NoOp -count=5`), and — to positively rule out this session's own diff — reproduced identically after `git stash`-ing every change back to the unmodified base branch and re-running `make test`. Confirmed still open and still purely load-triggered, not tied to any particular diff, on `main` as of that point.
- 2026-08-06, while running `go test -race ./session/... ./server/... -count=10` (a full, unusually heavy sweep well beyond normal `make quick-check` load) as the `tmux-ptmx-race-fix` backlog item's AC2 verification: the same load-triggered signature recurred but broader — `TestGetCloneURL_UsesHostSpecificURL` (`session/repo_path_test.go`) and `TestHandleActuatorHealth_ReturnsOK_InNormalConditions` (`server/actuator_test.go`) failed repeatedly across the sweep's 10 iterations, and `session/unfinished/gogitstore` plus `server/services` (an `AnalyticsStore.flush` goroutine stack was visible in the timeout dump) each hit the 10-minute `go test` timeout outright. Both failing tests confirmed passing reliably in isolation (`go test -race -count=5`, 5/5 each). Confirmed unrelated to that item's diff (scoped entirely to `session/tmux/tmux.go`, `session/tmux/tmux_test.go`, `server/server_integration_test.go`, `Makefile` — none of the failing tests' or timed-out packages' files were touched). A genuinely new, distinct data race (not this bug's tmux-binary-resolution signature) was also caught in the same sweep, on `Instance`'s status field between `transitionTo()` and an async `resumeFromHibernation()` — filed separately as backlog item `c23a9b30-62f3-4739-850f-6c1ae7025bd3` rather than folded in here, since it's a real correctness bug, not this bug's test-infra flakiness. The two outright timeouts (`gogitstore`, `server/services`) were not further isolated in this session — noted here rather than silently dropped, per this repo's own convention, but not yet root-caused.

- 2026-08-16, while running a full-suite retest (`go test $(go list ./...)`) as part of verifying the flaky-`TempDir`-cleanup-race fix for backlog item `e8d180d4-c4ff-4856-bc43-768365584420`: `TestKillOrphanedControlModeClients` (`session/tmux/kill_orphaned_control_mode_clients_test.go`) failed with a new signature — `signal: killed` after 12.64s, consistent with a `context.WithTimeout`-wrapped tmux subprocess call hitting its deadline under resource contention, not the binary-version-mismatch or count-mismatch signatures from the original report or this bug's other recurrence entries. Reproduced 10/10 passes in isolation (`go test -run TestKillOrphanedControlModeClients ./session/tmux/... -count=5`), confirming it's load-triggered like every other entry in this log. Confirmed unrelated to that item's diff — none of the seven files staged for that fix (`testutil/tmuxreap/tmuxreap.go`, `config/config.go`, `session/instance.go`, `server/services/session_service.go`, `server/services/backlog_service_test.go`, `session/session_driver.go`, `docs/bugs/open/BUG-077-...md`) touch `session/tmux`. Not further isolated beyond the above; logged per this bug's own convention of tracking recurrences rather than re-excusing them.

- 2026-08-19/2026-08-20, while validating `make test` for `PerfFix-2` (session/search CSR-flattened `PostingsList`) on branch `stapler-squad-perf`: a fifth recurrence, this time with a different set of test names again — `TestDestroy_should_CaptureDiffStatsBeforeCleanupWorktree_When_UpdateDiffStatsRunsFirst` and `TestBuildLaunchCommand_LargePromptSurvivesRealTmuxNewSession` (package `session`), plus `TestListStaplerSquadSessions_UsesIsolatedSocket` and `TestListStaplerSquadSessionsWithInfo_UsesIsolatedSocket` (package `session/mux`). All four passed 100% in isolation and failed only under the full `go test -short ./...` suite. Confirmed unrelated to the `PerfFix-2` diff (scoped entirely to `session/search`). Root cause this time: fixed wall-clock budgets (`destroyChainTimeout=5s` in `session/instance.go`, a hand-rolled 5s poll deadline, and two 10s `context.WithTimeout`s in `session/mux/multiplexer.go`/`picker.go`) getting blown by subprocess fork/exec scheduling delay — `session`/`session/mux` are two of the few packages that fork real `git`/`tmux` subprocesses at high `t.Parallel()` fan-out, and `make test`'s `go test -short ./...` invocation had no package-scoped parallelism cap for them (only `test-integration` and CI's `-race` coverage step had one, and CI's step didn't cover `session/mux`). Also found (same root-cause shape as this bug's original fix): `session/instance_tmux_command_length_test.go` still hardcoded the literal `"tmux"` binary in three `safeexec.CommandContext` calls instead of `tmux.Binary()`, bypassing the exec gate's throttling and `TMUX_BIN`/pinned-3.4-binary resolution.
  **Structural fix applied** (not just widened timeouts — see `project_plans/flaky-hook-url-tests/decisions/ADR-001-p1-flag-over-isolated-invocation.md`, which already ruled timeout-widening alone symptom-only for the sibling `test-integration` case): the `test` Makefile target now runs `./session ./session/mux ./session/tmux` with `-p 1` (serialized against each other only, mirroring `test-integration`'s existing precedent) in a first invocation, and every other package in a second, still-parallel invocation; `STAPLER_SQUAD_TMUX_CREATE_TIMEOUT_SECONDS=30` is now set for local `make test` to match CI's existing override, closing the local/CI timeout-headroom gap; and the three hardcoded `"tmux"` literals in `instance_tmux_command_length_test.go` were replaced with `tmux.Binary()`. Production safety-valve timeouts (`destroyChainTimeout`, mux list-session timeouts) were left unchanged in source — no override seam was needed. The same `-p 1` scoping (plus the `STAPLER_SQUAD_TMUX_CREATE_TIMEOUT_SECONDS=30`/`TMUX_BIN` env) was also applied to the `test-coverage` and `test-race` Makefile targets (each split into the same two-invocation pattern, with `test-coverage`'s two `-coverprofile` outputs manually concatenated into one `coverage.out`), and `testutil` (`TestRealTmuxSessionLifecycle`, which forks real tmux subprocesses the same way) was added to the `-p 1` group in all three targets — it hit the identical contention pattern under `make quick-check`'s `test-coverage`/`test-race` phases.

  A sixth test, `TestSessionDriver_DialogGaveUp_FallsThroughToInactivityEscalation` (`session/session_driver_test.go`), surfaced during validation of the above fix: it failed once at 68.52s against a self-computed `2*driverReadyTimeout + driverPollInterval*3 + time.Second` (67s) deadline under `session`'s own in-package `-p 1`/`t.Parallel()` load, and log evidence showed the underlying driver logic correctly escaped its retry loop ~4.5s after the test's own deadline fired — a test-margin problem, not a functional regression. Confirmed 5/5 passing in isolation. Fixed by widening the test's own deadline multiplier from `2*driverReadyTimeout` to `3*driverReadyTimeout` (67s → 97s), per the test's pre-existing doc-comment guidance that the 30s `driverReadyTimeout` wall-clock wait is inherent to the code path under test, so widening the margin (not retrying) is the correct fix. Verified after the fix: a full `make test` run's first (`-p 1`) invocation — `session`, `session/mux`, `session/tmux` — passed clean in 142.753s (`/tmp/make-test-4.log`, 2026-08-20).

  That same full run's second invocation separately failed two unrelated `server/services` tests under full-suite parallel load — same failure *class* (fixed wall-clock budgets/event-ordering assumptions under scheduler contention) but a different package not covered by this bug's `-p 1` scoping, and confirmed passing 5/5 in isolation. Not folded into this bug's fix (different package, different root cause per test, would have expanded that change's blast radius) — filed separately as `docs/bugs/open/BUG-083-server-services-flaky-under-full-suite-parallel-load.md` per this repo's fix-flaky-tests-dont-defer convention.

  **Before/after wall-clock, per ADR-001's precedent of not absorbing `-p 1`'s cost silently.** "Before" = the pre-fix `TMUX_BIN=$(pwd)/bin/tmux go test -short -timeout=20m ./...` single unscoped invocation (reconstructed manually against the current worktree, since the Makefile's own recipe had already been rewritten on this branch — see `/tmp/before-test.log`, 2026-08-20): `10:17.55 total` wall-clock (`239.06s user 220.48s system 74% cpu`), and it reproduced the flake class directly — `TestDestroy_should_CaptureDiffStatsBeforeCleanupWorktree_When_UpdateDiffStatsRunsFirst`, `TestBuildLaunchCommand_LargePromptSurvivesRealTmuxNewSession`, plus a third sibling in the same package/root cause (`TestDestroy_should_SkipDiffStatsCapture_When_NoLifecycleListenerRegistered`) and `session/tmux`'s two long-recurring flakes named in `.claude/rules/fix-flaky-tests-dont-defer.md` (`TestKillOrphanedControlModeClients`, `TestEnsureServerRunning_NoOp`) — plus BUG-083's and BUG-084's out-of-scope failures in `server/services` and the root package. "After" = the fixed two-invocation `-p 1`-scoped recipe: first invocation (`session`, `session/mux`, `session/tmux` under `-p 1`) `142.753s` (`/tmp/make-test-4.log`, 2026-08-20); the second (unscoped, everything else) is unaffected by this change and runs in parallel as before. Net: the `-p 1` scoping adds serialization only within the three tmux-heavy packages, and the wall-clock cost of that serialization (142.753s to run what previously ran as part of a 617.55s unscoped whole) is a small fraction of total suite time, not a suite-wide `-p 1` tax — consistent with ADR-001's rejection of repo-wide `-p 1`.

- 2026-08-21, while running `go test -race -tags integration -count=20/30/40` as the `fix-pty-triple-toctou-race` backlog item's AC5 verification: `TestTmuxServerRegistry_ConcurrentSubscriptions` (`session/tmux/server_registry_integration_test.go`) flaked at ~15-30% under a heavily shared/contended dev sandbox (2-4 concurrent unrelated agent processes competing for CPU throughout, confirmed via `ps aux`/`uptime`). Root-caused (not just re-excused) to three compounding issues, all fixed: this branch predated main's `-p1` test-integration serialization fix (commit `97cbcea8f`) by ~300 commits and never had it; the test waited on 10 subscriber channels sequentially against one shared `time.After(registryPollTimeout)` instead of concurrently against their own, letting one slow reconnect cascade into spurious failures for channels checked later in the loop; and `startIsolatedRegistry`'s keepalive-session bootstrap only checked `new-session`'s exit code, not that the server stayed queryable afterward — confirmed (by temporarily raising `registryPollTimeout` to 15s) that once `list-sessions` starts failing on a freshly-created isolated socket, it never recovers within a test's short lifetime. Added a stability probe (several consecutive successful `list-sessions` calls) before handing the server to the registry. Residual failure rate after all three fixes: ~6% on this same contended sandbox — a real, dedicated CI runner should see this meaningfully lower.

## Verification: 10 consecutive post-fix `make test` runs (2026-08-20/21)

Per this bug's own convention of citing concrete evidence rather than a prose claim of "it's fixed," ran `make test` 10 times in a row after the `-p 1` scoping fix landed. In every run, `session`, `session/mux`, `session/tmux`, and `testutil` — the four packages carrying this fix — reported `ok`, and none of the four originally-named flaky tests (`TestDestroy_should_CaptureDiffStatsBeforeCleanupWorktree_When_UpdateDiffStatsRunsFirst`, `TestBuildLaunchCommand_LargePromptSurvivesRealTmuxNewSession`, `TestListStaplerSquadSessions_UsesIsolatedSocket`, `TestListStaplerSquadSessionsWithInfo_UsesIsolatedSocket`) appeared in any `FAIL` line:

| Run | session | session/mux | session/tmux | testutil | Other failures in same run (all pre-existing, out-of-scope, filed separately) |
|---|---|---|---|---|---|
| 1 | ok 142.753s | ok (cached) | ok (cached) | ok (cached) | none |
| 2 | ok 120.597s | ok 3.456s | ok (cached) | ok (cached) | none |
| 3 | ok 337.270s | ok 15.180s | ok (cached) | ok (cached) | none |
| 4 | ok 106.258s (quick-check) | ok 14.658s | ok (cached) | ok (cached) | none |
| 5 | ok 102.331s (quick-check) | ok 8.213s | ok (cached) | ok (cached) | `TestWatchBacklogItems_should_deliverRaceWindowEventExactlyOnceAsSnapshot_When_PublishedBetweenSubscribeAndEventsSince` (server/services, BUG-083 class) |
| 6 | ok 462.521s (quick-check, uncached rerun) | ok (cached) | ok (cached) | ok (cached) | `TestListWorktrees_EmptyPath` (server/services, BUG-083) |
| 7 | ok 178.836s | ok (cached) | ok (cached) | ok (cached) | none |
| 8 | ok (cached) | ok (cached) | ok (cached) | ok (cached) | `TestWatchBacklogItems_should_deliverRaceWindowEventExactlyOnceAsSnapshot_When_PublishedBetweenSubscribeAndEventsSince` (server/services, BUG-083 class) — `make test` exit 2 came solely from this unrelated failure |
| 9 | ok (cached) | ok (cached) | ok (cached) | ok (cached) | none — exit 0 |
| 10 | ok (cached) | ok (cached) | ok (cached) | ok (cached) | none — exit 0 |

Runs 4-6 are `make quick-check` invocations (build+test+lint), so they also directly confirm `make quick-check` passes with this fix in place — run 6's tail output: `✅ Registry validation passed. Divergence: 0.49%` / `✅ Quick validation complete`, i.e. the full build+test+lint+registry pipeline completed successfully, not just the test phase in isolation.

- 2026-08-24, while validating a `stapler-squad-perf` CI-performance change to `.github/workflows/build.yml` (splitting the CI-wide blanket `-p 1` into a `session`/`session/mux`/`session/tmux` group at `-p 1` plus a fully-parallel second invocation for everything else, mirroring this bug's own Makefile fix): a local run of the exact CI split (`TMUX_BIN=$(pwd)/bin/tmux go test -race -p 1 -timeout=20m ./session ./session/mux ./session/tmux`, no `-short`) failed four tests, all in `session` itself — `TestFromInstanceData_ActiveSession_DetectsAlreadyRunningTmux` (`session_restart_test.go:700`), `TestLoadInstances_DoesNotBlockOnStartingActiveSessions` (`session_restart_test.go:791`), `TestSessionCreationDoesNotHang` (+ subtests `DirectorySessionCreation`/`ClaudeSessionCreation`), and `TestSessionCreationWithRealPrograms` (+ subtests `Bash`/`Claude`) — all four with this bug's exact core signature, `error starting tmux session: server exited unexpectedly (exit status 1)`, immediately after a `"creating tmux session"` log line. `session/mux` and `session/tmux` both passed clean in the same run. Confirms the contention is **intra-binary**: all four failing tests, and the `TestContextOptionsMerge`/`TestStartupScanner_Scan_SkipsSessionWithNoApproval`/etc. tests visible fanning out around them in the log, are `t.Parallel()` subtests forking real tmux servers concurrently *within the single `session` package binary* — package-level `-p 1` (already in effect for this group per this bug's original fix) cannot serialize against that, since `-p 1` only serializes *between* package binaries, not `t.Parallel()` subtests within one. System memory pressure (`"used_pct":92`) was logged in the same window, consistent with this bug's established "resource exhaustion under heavy concurrent tmux/git subprocess forking" root cause. Not a regression from the `-p1`-split change itself — all four tests are normally skipped by `make test`/`make quick-check` via their `if testing.Short() { t.Skip(...) }` guards (Makefile passes `-short`; `.github/workflows/build.yml` never has, in either the old blanket-`-p1` form or the new split), so this exposure is pre-existing in CI and orthogonal to the `-p1` scoping change under validation — but it's a newly-observed data point that CI's lack of `-short` makes it exercise (and be exposed to this bug's contention class via) real-tmux integration tests that local `make test` normally skips. Not further isolated in this session (single run, not yet reproduced in isolation or repeated); flagged here per this bug's own convention rather than silently excused, and left as a follow-up: either scope `t.Parallel()` off these four specific real-tmux-forking tests, or evaluate adding `-short` to CI's `build.yml` (tradeoff: would also drop CI's coverage of these integration-style paths, currently uncovered by local `make test`).

- 2026-08-24, follow-up fix to the four tests named in the 2026-08-24 entry above (`TestFromInstanceData_ActiveSession_DetectsAlreadyRunningTmux`, `TestLoadInstances_DoesNotBlockOnStartingActiveSessions`, `TestSessionCreationDoesNotHang`, `TestSessionCreationWithRealPrograms`): root-caused rather than left as a follow-up, per `.claude/rules/fix-flaky-tests-dont-defer.md`. `TmuxSession.start()` (`session/tmux/tmux.go`) called `preconfigureServerBeforeSession()` — a void, error-swallowing method that ran `start-server ; set-option ...` with no retry — and then proceeded unconditionally into `new-session -d` regardless of whether that preconfigure step actually succeeded. Under the concurrent-tmux-spawn contention this bug already documents, a transient `preconfigureServerBeforeSession` failure left the server not actually up, so the subsequent `new-session -d` failed with this bug's exact signature, `"server exited unexpectedly (exit status 1)"` — but with no retry budget behind it, unlike every other tmux-server-start path in this file (`EnsureServerRunning`/`ensureServerRunningWithRetry`, `serverStartAttempts=8`, `serverStartBackoffStart=100ms`, `serverStartBackoffMax=3s`, ~9.1s worst-case). **Fix:** converted `preconfigureServerBeforeSession` to return `error` and wrapped it in the same `ensureServerRunningWithRetry` machinery the rest of the file already uses; `start()` now aborts with a wrapped error instead of proceeding on a silently-failed preconfigure. This did not require adding new dedicated-tmux-daemon isolation — `testTmuxSocket(t)` (`session/session_creation_test.go`) already gives each of these tests its own PID-and-test-name-scoped tmux socket; the bug was purely the missing retry on the preconfigure step, not shared/colliding servers. After this fix, the same four tests began failing on a second, separate axis: hardcoded 5s/10s `require.Less` wall-clock assertions in `session/session_creation_test.go`, now legitimately tripped by the retry/backoff succeeding correctly under `t.Parallel()` contention (confirmed via a `-parallel 1` control run: all four passed in 4.83s with no retries needed). Fixed by widening the three assertions to 15s/15s/20s with comments citing the exact retry-budget math, consistent with this bug's existing precedent of widening test margins (not retrying) when the underlying wait is inherent to the code path under test (see the `TestSessionDriver_DialogGaveUp_FallsThroughToInactivityEscalation` entry above). Verified: all four target tests pass in 3 consecutive fresh `-count=1` runs plus one `-v` run (1.4s-2.3s each, no contention observed locally); the full `-p1`-split group (`./session ./session/mux ./session/tmux`) passes clean under `-race -p 1 -timeout=20m` (`session` 176.796s, `session/mux` 6.488s, `session/tmux` 53.243s); `go build ./...` clean across the whole repo.

## Resolution

Fixed on `main` by `dccee742a` ("fix(tests): fix flaky/failing tests across executor, tmux, gogitstore, and git packages", 2026-08-04) — root cause was several `session/tmux` test files hardcoding the literal `"tmux"` binary in `exec.Command`/`safeexec.CommandContext` calls, resolving to whatever `tmux` is on `$PATH` instead of the test-pinned `TMUX_BIN` (`bin/tmux`) the package's own `Binary()` helper resolves to — causing protocol/version mismatches when both connect to the same socket, plus widened retry logic in `tmux.go` for transient contention under heavy system load. Verified after merging that commit into `stapler-squad-fix-idle-reviewer-wedge`: full `go test $(go list ./...)`, including `session/tmux`, passed clean with no `-run`/isolation workaround needed.
