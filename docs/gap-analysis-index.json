{
  "titles": [
    "Five PR management RPCs have no frontend caller",
    "`TriggerSync` RPC is registered but always returns `CodeUnimplemented`",
    "Workflow `session_type` field ignored by scheduler except for `one_off`",
    "`workflowMetaCache` never refreshed after workflow CRUD \u2014 new workflows immediately auto-archived despite `archive_after_hours` setting",
    "`SESSION_STATUS_RESTORING` defined in proto but missing from generated Go and TypeScript bindings",
    "`ForkSession` goroutine omits all lifecycle wiring before starting the forked session",
    "`resumeFromHibernation` does not restart the controller after re-launching the session process",
    "AutonomousMode flag is never written to the ent database \u2014 lost on server restart",
    "`SessionWizard` component is unreachable: not imported by any page or route",
    "`session-create-wizard.spec.ts` e2e tests target UI that no longer exists",
    "Cron workflow scheduler never sets `one_shot` mode \u2014 sessions never auto-exit",
    "`ResumeHibernatedSession` never re-adds session to review-queue poller or rewires service callbacks",
    "`ForkSession` does not inject `MCPServerURL` into the forked instance before `Start()`",
    "`fork_source_id` combined with `SESSION_TYPE_NEW_WORKTREE` causes `--resume` to fail due to JSONL path mismatch",
    "`Header.tsx` is never imported in the running app \u2014 orphaned since CockpitShell refactor",
    "`WorkspaceSwitcher` is only rendered inside the dead `Header` \u2014 workspace switching is inaccessible",
    "`ApprovalDrawer` and `ApprovalNavBadge` are dead \u2014 quick tool-call approval is inaccessible from any page",
    "`HibernationSweeper` auto-hibernates sessions without stopping the `AutonomousDriver` goroutine",
    "Retention enforcer and `ArchiveWorkflowSessions` both archive Hibernated workflow sessions",
    "`ResumeHibernatedSession` skips all callback wiring and review-queue re-add",
    "`ForkSession` goroutine skips all post-Start wiring (status manager, rate-limit callbacks, controller)",
    "`ProfilesManager` silently clears `envVars` and `cliFlags` on every profile save",
    "UpdateProject ignores req.Msg.Id and looks up project by new name, breaking renames",
    "BacklogItem.user_modified_fields is read by sync loop but never written by UpdateBacklogItem",
    "maybeAutoArchive immediately archives workflow sessions when only keepSessions is set, defeating count-based retention",
    "`ResumeHibernatedSession` orphans the session from the live poller",
    "Resolved session defaults for cli_flags, env_vars, and tags are silently dropped during CreateSession",
    "CreateSessionRequest.profile and skip_defaults are never forwarded by useSessionService",
    "`WorkflowProto` has no `permission_mode` field \u2014 cron sessions always use default permissions",
    "fork_at_message=0 creates empty conversation fork instead of copying all messages",
    "`UpdateProject` handler ignores the `id` field from `UpdateProjectRequest`",
    "`UpdateProject` ignores `id` field \u2014 storage cannot identify the target project",
    "`SessionDefaults` `EnvVars` and `CLIFlags` fields are configured but never applied to sessions",
    "`ResumeHibernatedSession` does not re-start `AutonomousDriver` when `AutonomousMode=true`",
    "`ListBacklogItems` never populates `item_sessions` \u2014 board shows no triage/gate status",
    "`ForkSession` handler skips all post-creation wiring that `CreateSession` performs",
    "`GlobalDefaultsForm` silently drops `auto_yes` \u2014 never reads or saves it",
    "Scheduler `FireNow` ignores `session_type` for `new_worktree`/`existing_worktree`/`new_project` workflows",
    "`ResumeHibernatedSession` does not re-add session to poller, wire status manager, or restart autonomous driver",
    "`GlobalDefaultsForm` omits `autoYes`: saving global defaults silently resets it to false",
    "Project `running_count` / `complete_count` / `review_ready_count` always zero",
    "No dedicated Projects management page",
    "Backlog item source CRUD RPCs have no frontend UI",
    "`/settings/unfinished` page exists with real component but has zero inbound links",
    "`/account` page hardcoded in Header/BottomNav, bypassing `NAV_PAGES`",
    "`GlobalDefaultsForm` silently drops `autoYes` on every save",
    "`HibernationConfig` struct fields have no proto RPC and no settings UI",
    "`WorkflowForm` has no UI control for `session_type` \u2014 always submits `'directory'`",
    "`UpdateSession` drops `steer_message` silently when controller is nil",
    "`Session.DiffStats.content` is never populated in `InstanceToProto()`",
    "`browser-passthrough` feature flag has no `FEATURE_META` label and no frontend consumer",
    "`AnalyticsEvent` REST summary endpoint never read by any frontend component",
    "ClaudeSession.settings fields stored in DB but never exposed via proto response",
    "Session.cdpState populated server-side but never read by any frontend component",
    "`config.json` `anthropicApiKey` field silently ignored by AI rule generation service",
    "`sessionTypeToProto` adapter silently maps `SessionTypeNewProject` to `SESSION_TYPE_UNSPECIFIED`",
    "`CreateSessionRequest.allowed_tools` fully implemented in Go but unreachable from any frontend creation flow",
    "Session defaults `env_vars` and `cli_flags` are resolved and stored but never forwarded to the session process",
    "`CreateSessionRequest.profile` field is supported by the backend but silently dropped by the Omnibar session creation path",
    "`DrawerNav` renders feature-gated pages regardless of flag state",
    "Workflows have no `permission_mode` or `allowed_tools` fields \u2014 cron runs block on permission prompts",
    "`BatchCreateSessions` silently drops `initial_prompt` and `tags` from `BatchSessionRequest`",
    "`AutonomousDriver` completion clears `AutonomousMode` in-memory only \u2014 not saved to storage",
    "`WatchSessions` initial snapshot includes archived sessions",
    "`ArchiveSession` and `UnarchiveSession` do not emit EventBus events",
    "`Shell.stopped_at` defined in proto and DB but never populated in `shellToProto`",
    "`WorkflowsPanel` 'Run' button always fires with empty arg \u2014 breaks `{{input}}` workflows",
    "`CreateCheckpoint` returns an opaque error for Hibernated sessions because `i.started` is false after hibernation",
    "`ForkSession` does not register the new instance with `HistoryLinker`, so its JSONL file is never auto-linked",
    "`Session.launch_command` never populated in `InstanceToProto` \u2014 `SessionDetailView` always shows empty command",
    "`CheckpointList` component is orphaned and references a `DeleteCheckpoint` RPC that does not exist",
    "Logs page uses deprecated single-level field and client-side multi-level filtering instead of the proto-native `levels` repeated field",
    "`DebugMenu` is only in the dead `Header` \u2014 the `/debug/escape-codes` page is unreachable",
    "`ConnectionIndicator` and `MemoryNavBadge` are dead \u2014 WebSocket status and memory pressure are invisible",
    "`AnalyticsEvent` ent entity is defined but `LogClientEvents` never persists to it",
    "`EntRepository.ListSessions()`, `CreateSession()`, and `UpdateSession()` always return unimplemented error",
    "Omnibar form ignores `SessionDefaults.Program` when opening",
    "`memory_rss_mb` and `estimated_savings_mb` only populated in `ListSessions`, not in `GetSession` or `WatchSessions`",
    "`UpdateSession` Pause path does not stop or deregister the `AutonomousDriver`",
    "`UnfinishedWorkService.UndismissWorktree` has no frontend UI",
    "`/backlog/board` page bypasses the backlog feature flag and uses a hardcoded href instead of the route constant",
    "`GlobalDefaultsForm` does not load or save the `auto_yes` global default",
    "`CreateSession` with `autonomous_mode=true` does not register the turn callback",
    "`BacklogSyncLoop` silently no-ops when storage backend is not `*EntRepository`",
    "`VCSPreference` (jj vs git selection) has no proto representation and no settings UI",
    "`workflowMetaCache` populated once at startup and never refreshed, despite comment claiming 'every minute'",
    "`reconcileSessions` ignores Paused sessions, leaving zombie Paused+no-tmux state indefinitely",
    "WatchInsights silently ignores from/to time-range filter from client",
    "BacklogItem.user_modified_status_at is write-only: stored but never queried or exposed",
    "BacklogItem\u2192Session many-to-many ent edge is defined in schema but never populated or queried",
    "`ArchiveSession` and `UnarchiveSession` RPCs implemented in hook but never called by any component",
    "ResolveDefaults RPC is never called in the live Omnibar session-creation path",
    "Retention enforcer archives sessions in DB but never updates in-memory reviewQueuePoller",
    "WorkflowProto has no existing_worktree path field, making session_type=existing_worktree unusable",
    "session_driver.go `isOneShot()` checks backlog tags but ignores `inst.OneShot`, causing one-shot sessions to be incorrectly restarted",
    "`ResumeHibernatedSession` does not restart the AutonomousDriver for sessions where `autonomous_mode=true`",
    "BacklogService ItemSource CRUD RPCs implemented but unreachable from frontend",
    "SESSION_TYPE_NEW_PROJECT not mapped in session type adapters \u2014 round-trip lossy",
    "`BacklogService` item source CRUD has no frontend callers",
    "`UndismissWorktree` RPC is implemented but the Unfinished Work UI has no way to undo a dismiss",
    "`AutonomousDriver` not restarted when resuming a hibernated autonomous session",
    "ClaudeSession.metadata map stored in DB but never copied into proto response",
    "WorkflowProto has no autonomous_mode field \u2014 FireNow can't start AutonomousDriver",
    "`BacklogService.AttachSessionToItem` is implemented in Go but missing from `useBacklogService` hook",
    "`/account` page exists but is absent from `NAV_PAGES`",
    "`SessionWizard` applies only `program`/`autoYes` from `ResolvedDefaults`; `tags`, `cliFlags`, and `envVars` are ignored",
    "`UnfinishedSourcesSettings` page is unreachable \u2014 no navigation link exists",
    "`UpdateSession` silently ignores `SESSION_STATUS_HIBERNATED` and `SESSION_STATUS_RESTORING` status transitions",
    "`WatchInsights` and `WatchUnfinishedWork` missing from `StreamingWSBridge`",
    "`ArchiveSession`/`UnarchiveSession` RPCs implemented but have no UI entrypoint",
    "`GetSyncHistory` is stubbed but the storage layer has no corresponding read method",
    "`config.NotificationPrefs.PushEnabled` is defined but never consulted by `PushService`",
    "`WorkflowProto` has no `autonomous_mode` field \u2014 workflow-spawned sessions cannot self-complete",
    "`ResumeHibernatedSession` does not publish an `AddInstance` event, so `WatchSessions` clients never see the revived session",
    "`UpdateSession` resumes Paused sessions when any non-PAUSED status is sent, not just ACTIVE",
    "`UpdateSession(autonomous_mode=true)` on a non-Active session sets the flag but silently fails to start the driver",
    "`ListSessionsRequest.project_id` filter is never applied by the handler",
    "`LogClientEvents` handler logs to stderr only, never writes to `AnalyticsEvent` DB table",
    "`ForkedFromID` is persisted in JSON storage but never surfaced in the proto `Session` message",
    "`ListSessionsRequest.hide_paused` filter accepted but silently ignored by server",
    "`UpdateSession` silently ignores `status=SESSION_STATUS_ACTIVE` for Stopped sessions despite the state machine allowing Stopped\u2192Active",
    "`DetectionEventsPanel` only accessible via `?debug=1`",
    "`LogUserInteraction` audit trail is write-only",
    "Dual escape analytics systems with separate stores",
    "Duplicate `LogViewer` component sets",
    "`WatchInsightsRequest` `from`/`to` time-range fields silently ignored",
    "`CreateSessionRequest.profile` field handled server-side but never sent from the live frontend",
    "`routes.settingsDefaults` is a dead constant \u2014 page just redirects and constant is never used",
    "Duplicate escape-code analytics pages with no cross-linking",
    "`/test-terminal` page undiscoverable \u2014 no nav entry or in-app link",
    "Config.Notifications.PushEnabled is persisted but never read by any server code",
    "`config.BranchPrefix` has no proto field and no settings UI",
    "Workflow `model` field is silently dropped when `agentType` is a non-Claude program",
    "`UpdateSession` status change from `HIBERNATED` to `ACTIVE` is a silent no-op",
    "`Session.claude_conversation_uuid` (field 42) never set in `InstanceToProto` despite data being available",
    "`/test/escape-codes` and `/test/layout-overlap` pages exist but have no route constants or nav entries",
    "`browser-passthrough` and `backlog:conversation-view` feature flags render as raw machine names in settings UI",
    "`DetectionEventProto.tail_snippet` never populated by `GetDetectionEvents` handler",
    "`SessionWizard` component is never imported or rendered anywhere in the application",
    "`logs/page.tsx` falls back to client-side multi-level filtering despite the server already supporting `levels[]`",
    "`ProtoToSessionType` maps `SESSION_TYPE_NEW_PROJECT` to `SessionTypeDirectory` instead of `SessionTypeNewProject`",
    "`VNC_STATUS_PASSTHROUGH` not handled in `BrowserTab` \u2014 falls through to 'No browser open yet'",
    "`ListBranchesRequest` filter/maxResults/includeRemote fields never sent from `useBranchSuggestions`",
    "`ListClaudeHistoryRequest.project` filter field never used by the history page",
    "`/backlog/board` page linked via hardcoded string instead of `routes.backlogBoard` constant",
    "`routes.settingsUnfinished` constant defined but never referenced anywhere",
    "`ListSessions` workflow_id filter not applied to external (mux) sessions",
    "`UpdateWorkflow` allows `cron_enabled=true` with existing empty `cron_expression` via partial update",
    "`ForkFromCheckpoint` does not copy `ProjectID` from source, losing project association on fork",
    "`ConditionalHeader.tsx` wraps the dead `Header` but is itself never imported",
    "`RunOneShotResponse.branch_diverged_from_base` silently dropped at review-queue adapter",
    "`WorkflowScheduler.eventBus` is injected but never used \u2014 no workflow-fired events published",
    "`ListSessions` accepts `hide_paused` and `search_query` proto fields but never applies them server-side",
    "`BacklogService.SuggestNextItem` implemented but no frontend entry point",
    "`useBacklogService` hardcodes `sortBy: ''` \u2014 server-side sorting never used",
    "`routes.settingsDefaults` is defined and has a redirect page but is never referenced in frontend code",
    "`/debug/escape-codes` is linked from `DebugMenu` with a hardcoded string but has no `routes.ts` entry",
    "Dev/test pages shipped in production with no `routes.ts` entries",
    "`ListSessionsRequest.search_query` filter field is accepted but never processed",
    "`DrawerNav` (desktop nav) has no Account link when auth is enabled",
    "ReviewVerdict.prompt_hash is stored in DB but never read back or exposed in proto",
    "`ItemSession.last_progress_at` is written but never read or exposed",
    "`DeleteWorkflowFailedSessions` archives sessions in DB but does not update in-memory `reviewQueuePoller` state",
    "CreateSessionRequest.skipDefaults field never sent \u2014 frontend cannot bypass profile/directory defaults",
    "WatchReviewQueueRequest.sessionIds filter never populated \u2014 no per-session review queue watching",
    "\"Config Files\" nav item never highlights as active because its href includes a query param",
    "Both \"Settings\" and \"Features\" nav items highlight as active simultaneously when on /settings/features",
    "ResolveApprovalRequest.message field never populated \u2014 denial reason is always absent",
    "ResolveDefaultsResponse.matched_directory (field 9) silently dropped in useSessionDefaults hook",
    "one_shot is persisted to the DB and present in CreateSessionRequest but absent from the Session read model",
    "Settings program selectors use a static list and miss server-detected programs",
    "`WatchInsights` server-streaming RPC has no `StreamingWSBridge` \u2014 uses HTTP long-polling in browsers",
    "`ListSessionsRequest.projectId` filter exists on the server but the frontend never sends it",
    "Two backend feature flags lack friendly labels in the Features settings page",
    "`useSessionService.createSession` hook silently drops `oneShot`, `allowedTools`, and `projectId` fields",
    "/backlog/board page exists and is linked from the Backlog list page but has no direct nav entry",
    "WorkflowsPanel table omits model, agentType, and sessionType columns",
    "`ListSessionsRequest.search_query` and `hide_paused` fields are silently ignored",
    "`/backlog/board` linked via raw string instead of route constant; absent from nav",
    "`/debug/escape-codes` page exists with no route constant and no nav entry",
    "`listUnfinishedWork` one-shot RPC is unused; frontend only uses the streaming `watchUnfinishedWork`",
    "`/debug/escape-codes` page is reachable only via the hidden debug-menu wrench button",
    "`/test-terminal` page has no route constant and is discoverable only via direct URL or e2e tests",
    "`ListSessionsRequest.hide_paused` and `search_query` filters accepted but silently ignored by the handler",
    "`SUGGESTION_SOURCE_REVIEW_QUEUE_ITEM` / `analytics_item_id` silently ignored",
    "`DeletePromptHistory` RPC has no frontend hook wrapper or UI call",
    "`browser-passthrough` feature flag has no human-readable label in Features UI",
    "`DeletePromptHistory` RPC exists but frontend offers no way to delete prompt history entries",
    "`routes.backlogBoard` constant is defined but never consumed \u2014 `/backlog/board` is linked with a hardcoded string",
    "`Session.launch_command` is never populated \u2014 `SessionDetailView` always shows empty",
    "`?title=` query param built by `routes.newSessionFromWorktree` is silently dropped on the home page",
    "`ResolveDefaults` `fieldSources` for `autoYes`, `tags`, and `cliFlags` are computed but never surfaced with `SourceBadge` in `SessionWizard`",
    "`/test/layout-overlap` page exists with no inbound links and no e2e test coverage",
    "`SessionGoal` is loaded during `Storage.loadLiveSessions` but not during ent repository Get/List queries",
    "`Config.Notifications.PushEnabled` flag is stored but never checked by the push service"
  ]
}