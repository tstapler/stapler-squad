# Pre-mortem: ci-hookurl-race-flake
**Date**: 2026-08-01

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | This project's own `implementation/adversarial-review.md` and `architecture-review.md` (both verdict CONCERNS) already found 6 distinct, unremediated gaps — but `plan.md` was never edited to fold any of them in, and nothing routes a Phase 5 implementer to those two files. The standard reading path for "execute this plan" is `requirements.md` + `plan.md` (demonstrated by this very pre-mortem task's own instructions, which named only those two files); `implementation/adversarial-review.md`/`architecture-review.md` sit as siblings a fresh worker subagent has no structural reason to open. Every one of the 6 Concerns (stale/undecided 20-run citation, Task 3.1.3 mislabeled as "Risk Control," AC#2's PR-description requirements having no enforcement, the build-vs-buy rebuttal winning by authority not argument, the dangling cross-project ADR-001 comment reference, Task 2.1.2b's misfiled placement) ships exactly as-is. | The PR merges with `plan.md`'s Observability Plan / Risk Control sections still citing "the 20-run window named in requirements.md's Success Metrics" — a citation that resolves to nothing in this project's own `requirements.md`, exactly as the adversarial review already documented — and no reviewer catches it because the two review docs were never re-opened during implementation or PR review. | Before Phase 5 starts, either (a) merge each unaddressed Concern's recommended edit directly into `plan.md` so the executable spec is self-sufficient, or (b) add an explicit line to `plan.md`'s Provenance section — "Before implementing, read `implementation/adversarial-review.md` and `architecture-review.md` in full; 6 Concerns remain open and are not yet folded into the task breakdown below" — so the dependency is structural, not a hope that someone remembers to look. | P1 |
| 2 | `research/build-vs-buy.md`'s divergent recommendation (apply the repo's existing `//go:build integration`/`testing.Short()` convention to the two flaky tests) is rejected only narratively — in `plan.md`'s Unresolved Questions prose, using a rebuttal the project's own adversarial review already flagged as "winning by appeal to ADR-001's authority, not by refuting build-vs-buy.md's AC#3 framing on its own terms." Nothing enforces the rejection: no ADR entry under this project's `decisions/`, no CI lint rule, no test asserting the two functions carry no `//go:build`/`testing.Short()` guard. A future contributor who greps for "how do we usually isolate a slow/flaky test in this repo," finds `build-vs-buy.md`'s own recommendation (which reads as unconditionally "Recommended," not "considered and rejected"), and applies it on top of the already-shipped `-p 1` mitigation would silently move these two tests' coverage from gating to the advisory (`continue-on-error: true`) lane — a real regression that looks like routine cleanup, not a revert. | A later, unrelated PR adds `//go:build integration` (or a `testing.Short()` guard) to `TestServer_should_WriteUnchangedHookURL_When_StartedOnExplicitPort`/`TestServer_should_WriteRealPortIntoSessionHooksAndMCPURL_When_StartedWithPortZeroThenSessionCreated`, CI stays green (the advisory lane doesn't gate), and nobody notices the gating suite's `-race` coverage quietly dropped these two tests until much later, if ever. | Add the adversarial review's own recommended sentence directly rebutting `build-vs-buy.md`'s AC#3 framing (already drafted in `adversarial-review.md`'s Concern #4) into `plan.md`'s Pattern Decisions table itself, and add one line to `build-vs-buy.md` §2's verdict cross-referencing that this recommendation was evaluated and **not adopted** for `ci-hookurl-race-flake`, with a pointer to the rebuttal — so a future reader of `build-vs-buy.md` alone (not just `plan.md`) sees it was already considered and closed, not left open. | P1 |
| 3 | Task 2.1.2b is the *only* task addressing acceptance criterion 4 ("the hook-injection pipeline's actual latency... is measured, not assumed"), yet the plan's own "Critical path for merge" section explicitly lists it among tasks that "can be reviewed/merged in the same PR without blocking" — i.e., not required before merge. The PR can close this backlog item, and `validation.md`'s requirement-to-test mapping can claim AC#4 is "addressed," while the actual measurement never ran. | The ticket is marked done/merged; months later, someone re-litigating "is 60s still the right number" opens `validation.md` looking for the AC#4 latency distribution and finds none, because Task 2.1.2b was skipped as non-blocking and no gate caught the gap between "task exists in the plan" and "task's evidence exists in validation.md." | In Phase 4's `validation.md`, make Task 2.1.2b's output (the recorded latency distribution) an explicit, checked-off requirement-to-evidence mapping for AC#4 — not just a task listed in `plan.md` — and add one sentence to the plan's Risk Control section stating the PR must not be marked ready-to-ship until that evidence line in `validation.md` is filled in, even though the *task* itself is off the critical path for code review purposes. | P1 |
| 4 | `flaky-hook-url-tests/decisions/ADR-001-p1-flag-over-isolated-invocation.md`'s `## Status` field still reads `Proposed` (confirmed unchanged in this plan's own Provenance section), and no task in this plan — or its sibling — flips it to `Accepted`/`Implemented` once `-p 1` actually ships. Yet this plan's authority chain leans entirely on ADR-001 being read as settled: the `build.yml` inline comment says "do not remove without re-reading that ADR," and Risk Control/Unresolved Questions both cite it as the closing argument against the build-tag alternative. A "Proposed, never applied" ADR reads as non-authoritative to a future reader, undermining exactly the guardrail the comment relies on, and makes the whole `flaky-hook-url-tests/` directory (which the architecture review already flagged as a soft dependency this plan points into) look like an abandoned proposal rather than a live, shipped decision — raising the odds it gets pruned or dismissed later. | A future contributor reads `ADR-001`'s `Status: Proposed`, concludes the `-p 1` change was never actually agreed to or implemented (despite it being live in `build.yml`), and either re-opens the build-tag-vs-`-p 1` debate from scratch or deprioritizes maintaining the ADR's currency because it "isn't real" per its own status field. | Add a task (or a one-line addendum to Task 2.1.1) that updates `flaky-hook-url-tests/decisions/ADR-001-p1-flag-over-isolated-invocation.md`'s `## Status` to `Accepted` (or `Implemented`, with a link to the merging PR) as part of the same PR that lands `-p 1` in `build.yml` — so the ADR's own status field never lags behind the code it authorizes. | P2 |
| 5 | Every task in Epic 1/3 anchors its edit instructions to literal line numbers (e.g., "lines 338, 409," "lines 454-462") re-verified as accurate "as of 2026-08-01." If Phase 5 implementation doesn't start immediately — plausible for a backlog item awaiting scheduling — and `server/server_integration_test.go` drifts in the meantime (it already has once: PR #190 touched nearby files in the same area per `requirements.md`'s own Evidence section), a fresh task-scoped subagent that trusts the plan's "no drift found, all references verified" framing rather than re-grepping by content (e.g., the literal `30*time.Second` string) before editing risks applying Task 1.1.1's `30→60` change to the wrong call site, or Task 1.2.1's rewrite to a function that has since moved. | A task's diff doesn't match its own Given/When/Then description on review — e.g., Task 1.1.1's PR diff shows a change at a line number that no longer corresponds to `waitForPermissionRequestHookCommand`'s call site, or the diff touches unrelated code the line number now points at. | Add one line to `plan.md`'s Step 4 preamble: "Line numbers throughout this section were verified current as of 2026-08-01; before editing, re-grep for the anchor text (e.g., `waitForPermissionRequestHookCommand(t, settingsPath, 30*time.Second)`) rather than trusting the cited line number if implementation starts more than a few days after this date." | P2 |

## P1 Items (address before implementation)
- [x] #1 — Fold the unaddressed Concerns from `implementation/adversarial-review.md`/`architecture-review.md` into `plan.md` directly, or add an explicit "read these two files first" instruction to the Provenance section so Phase 5 cannot silently skip them. **Resolved**: added a "Required reading before Phase 5" subsection to plan.md's Provenance, and folded the two cheaply-actionable Concerns (Task 3.1.3 reframed as diagnosis-aid, PR-description must-includes list) directly into task text.
- [x] #2 — Move the adversarial review's already-drafted rebuttal of `build-vs-buy.md`'s AC#3 framing into `plan.md`'s Pattern Decisions table, and cross-reference the rejection from `build-vs-buy.md` §2 itself so a reader of either document sees the decision was made and closed, not left open for independent re-litigation. **Resolved**: new Pattern Decisions row added to plan.md; `research/build-vs-buy.md` §2's verdict now cross-references the rejection and rationale.
- [x] #3 — In `validation.md`, make Task 2.1.2b's latency-distribution output a checked, evidence-backed requirement mapping for AC#4 (not just a listed task), and state explicitly that the PR is not ready-to-ship until that evidence is recorded, despite the task itself being off the merge-blocking critical path. **Resolved**: validation.md's AC#4 row already ties to a concrete procedure (validation subagent output); plan.md's Risk Control section now has an explicit "AC #4 evidence gate" sentence stating the PR isn't ready-to-ship until that evidence is recorded.
