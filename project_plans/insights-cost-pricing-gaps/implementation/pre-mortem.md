# Pre-mortem: insights-cost-pricing-gaps
**Date**: 2026-07-27

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | Task 1.1.1a's WebFetch succeeds but the implementing agent mistranscribes a number (transposed digit, per-1K vs per-1M confusion, or picks the promotional rate when steady-state was intended or vice versa) — and nothing catches it because the only "sanity check" is a format comparison against `claude-sonnet-4`'s numbers, and Task 1.1.1e's test hardcodes "the verified numbers" from the very same (possibly wrong) fetch, so the test is circular and cannot detect a transcription error, only a later code regression away from whatever number was typed in. | Sonnet 5 costs on the Insights dashboard are silently wrong by an order of magnitude (or by the promo/steady-state delta) — no error, no test failure, no log line; a human has to notice the dollar figure looks implausible. | Require a second independent confirmation before the number enters `pricing.go`: either a second `WebFetch` of the same page compared against the first, or a `WebSearch` for a corroborating announcement, with the two figures diffed before Task 1.1.1b writes the entry. Also change Task 1.1.1e's test from a bare hardcode to include a plausibility assertion (e.g. new price within an expected multiple of `claude-sonnet-4`'s price) so a gross unit/order-of-magnitude error fails even if the "verified" number was itself wrong. | P1 |
| 2 | The `<synthetic>` filter (Task 1.2.1a) is applied only at the two specific `parser.go` line numbers cited in the plan; if those lines have drifted since research, or if another model-aggregation site exists elsewhere in the codebase that the plan's grep didn't find, `<synthetic>` can still reach `PricingTable.Prices` lookup. Because Epic 1.3 now propagates *any* unpriced family straight to a loud UI badge (instead of the old silent `$0.00`), a `<synthetic>` leak that used to be invisible becomes a new, visible, confusing "pricing unavailable" badge — a regression this project itself introduces. Task 1.2.1b's regression test only proves the filter works for the one fixture it authored, not that `<synthetic>` can never reach `resp.UnpricedModels` end-to-end. | The Insights model-breakdown chart or legend shows a `"<synthetic> (pricing unavailable)"` entry — a brand-new, user-visible artifact that didn't exist pre-fix. | Add an end-to-end regression test in `insights_service_test.go`: a fixture mixing a `<synthetic>` turn with real turns run through the full `GetInsightsSummary` path, asserting `<synthetic>` never appears in `resp.UnpricedModels` or as a `ModelBreakdown` row — closing the gap between "filtered in the parser unit test" and "actually never surfaces on the wire," which is what a user would notice. | P2 |
| 3 | Epic 2.4's three frontend tasks (`DailySpendChart`, `ModelOverTimeChart`, `ProjectedCostCard`) were added in the repair pass, not the original Step 0.5 creative pass, and the adversarial review's own verification language ("closely enough to trust the diffs described") is hedged rather than a hard confirmation. In particular, `ModelOverTimeChart`'s legend may be library-rendered (e.g. a charting library's auto-generated `<Legend>` via a `formatter`/`content` prop) rather than the manually-mapped JSX list the plan assumes when it says to edit "the legend map (lines 154-162)" the same way `ModelBreakdownChart` was edited — if the real implementation differs, the task can't be applied as literally written. | `ModelOverTimeChart.test.tsx`'s new Jest test fails to find the `"(pricing unavailable)"` string in the legend, or the implementing agent has to improvise an insertion point the plan never described, producing an inconsistent visual treatment vs. `ModelBreakdownChart`. | Add a verification-only sub-task to Epic 2.4 (mirroring Task 1.3.1b's "verification only" pattern) that reads the actual current `DailySpendChart.tsx`/`ModelOverTimeChart.tsx`/`ProjectedCostCard.tsx` files and confirms the plan's cited line numbers and legend-rendering mechanism (manual map vs. library-managed) before any edit task begins — the same grep-verification discipline the architecture review already applied to the backend call sites, never required as an explicit plan step for these three frontend files. | P2 |
| 4 | Filing the ADR-002/ADR-003 fast-follow bugs (Tasks 3.1.1a/3.2.1a) is a "Files: none" task with no compiler/test gate — nothing in `make quick-check` verifies a backlog item was actually created, so under time pressure it's the easiest task in the whole plan to silently skip. Even if filed, its only durability mechanism is a PR-description mention (Task 3.1.1b/3.2.1b); once the PR merges and scrolls out of view, nothing keeps the two deferred pricing bugs visible. Six months later, someone reads this PR's "fix blank costs" title/history and reasonably assumes all Claude-model pricing gaps (including `capacity_monitor.go`'s live cost-budget trigger and Backlog's per-item cost display) were fixed here too. | A future investigation into a `capacity_monitor.go`-triggered autonomous-session pause, or a Backlog `$0.00` cost complaint, finds no linked backlog item at all, or finds one that was filed but never triaged/prioritized and has sat silently since 2026-07-27. | Make the filing step verifiable, not just prose: require Task 4.2.1a's final gate to include pasting the two backlog-item IDs (not just "reference the filed bug") verbatim into the PR description under the existing "Known related issue (not fixed here)" heading, so a reviewer or a future grep can confirm the artifacts exist rather than trusting that the step happened. | P2 |
| 5 | Task 4.1.1a/4.1.1b/4.1.2b are manual JSON edits where `testIds` strings are typed by hand rather than mechanically extracted from the test files that were just written. If a name is typo'd, or a test gets renamed during implementation but the registry JSON isn't updated to match, nothing described in Task 4.1.2c/4.2.1a's checks (`registry-generate`/`registry-diff`/coverage-gap count) appears to validate that a `testIds` string actually corresponds to a real Go/TS test function on disk — a self-consistent but factually wrong entry would pass every described gate. | `make quick-check` and `registry-diff` both report green, but a later audit (or simply `grep`-ing for the claimed test name) finds the registry references a test that doesn't exist or was renamed — the registry is internally consistent but not truthful, silently defeating its purpose as a queryable coverage source of truth. | Add an explicit sub-step to Tasks 4.1.1a/4.1.1b/4.1.2b requiring the implementer to `grep` the exact test function name in the actual test file immediately before pasting it into the registry JSON, and extend Task 4.2.1a's fallout-diagnosis list to include a grep-based cross-check of every `testIds` entry touched by this project against the real test files. | P3 |

## P1 Items (address before implementation)
- [x] Failure #1 (WebFetch pricing transcription error) — add a second independent verification source (repeat `WebFetch` or corroborating `WebSearch`) before Task 1.1.1b writes any pricing number into `pricing.go`, and replace Task 1.1.1e's circular hardcoded-number test with one that also asserts plausibility (e.g. bounded relative to `claude-sonnet-4`'s known-correct rate) so a gross transcription error fails even if the "verified" figure itself was wrong. **Resolved in plan repair (2026-07-27)**: `implementation/plan.md` now has a new Task 1.1.1a2 requiring a second independent confirmation (repeat `WebFetch` or corroborating `WebSearch`, diffed, mismatch escalated to the user) before Task 1.1.1b writes the number, and Task 1.1.1e's test description now requires an additional plausibility assertion bounding `claude-sonnet-5`'s rate relative to `claude-sonnet-4`'s known-correct rate.
