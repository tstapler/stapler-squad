# Architecture Review: network-hostname-redetect (re-review pass)
**Date**: 2026-09-18
**Verdict**: CONCERNS

## Previously-Blocked Items Re-Checked
- [x] Blocker 1 (manual-trigger channel) — RESOLVED — `manual chan chan redetectCycle` field defined in Task 2.1.1a (plan.md:252), consumed inside `Run`'s own `select` loop in Task 2.1.1b (`case respCh := <-d.manual: respCh <- d.redetect(ctx, TriggerManual)`, plan.md:257), and Task 4.2.2a's HTTP handler explicitly avoids calling `redetect` directly ("that would race `Run`'s own goroutine over the unsynchronized `d.networks` map — see the architecture review's Blocker A") and instead sends `respCh` on `detector.manual` with a 10s send-side timeout guard (plan.md:452, 459-460). No remaining path calls `redetect` from a goroutine other than `Run`'s own.
- [x] Blocker 2 (unconditional detector construction) — RESOLVED — Task 2.1.2c: "The detector **always starts**, unconditionally, in the `\"runtime\"` phase — regardless of `remoteAccessFlag`/`cfg.PasskeyEnabled`... Only `startRemoteAccess`'s own `if remoteAccessFlag || cfg.PasskeyEnabled` guard stays as-is (unchanged, still gates whether `waHandler`/`certStore`/`networks` exist at all)" (plan.md:287), with construction/`a.Go(...)` placed "outside and after that `if` block, so it runs on every install regardless of the remote-access/passkey setting" (plan.md:288). Only `Handler`/`CertStore` inputs are conditional (nil when disabled), and `redetect`'s nil-checks are corroborated by Story 2.2.1's acceptance criterion (plan.md:304-305). No stale/contradictory nested-construction text found elsewhere in the plan.

## New Issues Found During Re-Check (if any)
- [x] **RESOLVED (post-triad-review patch)**: Task 4.2.2a's manual-trigger handler previously guarded only the *send* to `detector.manual` with a 10s timeout, with no symmetric guard on the subsequent *receive* from `respCh`. This was independently re-surfaced by pre-mortem.md #3 during Phase 4 and has since been patched: Task 4.2.2a now applies the same `select`-with-timeout pattern to the receive (`select { case cycle := <-respCh: ...; case <-r.Context().Done(): ...; case <-time.After(10*time.Second): ... }`), and Task 4.2.2b adds `TestRedetectHostnamesEndpoint_RespChReceiveTimesOutIfRunStalled` to cover it. No longer an open finding — do not reopen without re-checking the current plan.md text.

## Carried-Forward Concerns (unchanged, not re-reviewed this pass)
Not enumerated in the prior review content available to this pass — the original architecture-review.md was overwritten by this re-review per instructions, and this pass was scoped only to verifying the 2 named blockers plus a sanity check of the fix itself. No other prior concerns/nitpicks were carried forward because their text was not available for verbatim inclusion here.
