# Pre-mortem: token-cost-tracking
**Date**: 2026-08-02

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | `SessionList`'s "Sort: Cost" view visibly re-sorts/jumps as live `WatchInsights` cost updates stream in while a user is looking at it — `compareSessionsByCost`'s early-return only guarantees "unloaded sorts last," not "no reordering once a row is loaded and its cost keeps changing" (e.g. a running session's cost climbing past a neighbor's). | User report/bug that rows "jump around" while sorted by cost, especially for in-progress sessions. | Add an explicit requirement to plan.md/validation.md to freeze sort order on a snapshot (re-sort only on explicit user action or a debounce window), not on every `WatchInsights` push; add a test asserting order stability across an incremental cost update. | P2 |
| 2 | Task 3.2.3b's outlier badge applies `className={badgeVariant.warning}` alone in the new per-turn table, omitting the shared `badge` base class that provides padding/pill-shape/inline-flex/border-radius. Verified against `web-app/src/components/shared/TokenBadge.tsx:47`, whose own composition is `[badge, badgeVariant[variant], className].join(" ")` — the plan's snippet doesn't match its own cited precedent. | Visual QA / code review on `SessionDetailDrawer`'s new table shows outlier Input/Output values as unstyled colored text with a stray border and no padding, inconsistent with every other badge in the app. | Update plan.md Task 3.2.3b to compose `className={[badge, badgeVariant.warning].join(" ")}` (importing both `badge` and `badgeVariant` from `TokenBadge.css`), matching `TokenBadge.tsx`'s exact pattern; assert both classes are present in the new test. | P2 |
| 3 | AC-5's stated proof — "run `make registry-generate`, confirm no unexplained growth in `coverage-gaps.json`" — cannot detect whether the 5 new hand-authored files under `docs/registry/features/frontend/` did anything. Verified against `tools/scanner/frontend/src/main.ts`, `component-scanner.ts`, and `gap-reporter.ts`: `coverage-gaps.json` is computed purely from live `// +feature:` marker scans of `.tsx` source, matched by domain-prefix — it never reads the per-feature JSON directory the plan is populating. | The before/after `coverage-gaps.json` diff shows zero change regardless of whether the new JSON files are correct, complete, or even exist — a false-positive "pass" with no signal either way. | Drop the coverage-gaps.json diff as AC-5's completion proof in plan.md/validation.md. Instead verify the new entries via `registry-aggregate`'s actual output (`docs/registry/frontend-features.json` assembled from the per-feature dir) or `make registry-diff`, and/or add distinct `// +feature: <id>` markers to the 5 source files if marker-driven coverage is the real intent of the rule. | P1 |
| 4 | `TestWatchInsights_should_forwardUpdateEvent_When_TokenStoreNotifies` depends on real-time `require.Eventually` polling against fsnotify-driven goroutine scheduling and `TokenStore.notify()`'s best-effort, non-blocking fan-out (`subChanSize=64`), which can silently miss a notification if the subscriber isn't registered yet when `OnHistoryFileChanged` fires. | Intermittent CI-only failures on this one test (passes locally, flakes under parallel CI load) with no code change — risks being retried/ignored rather than root-caused. | Add an explicit synchronization point before firing `OnHistoryFileChanged` (e.g. a short retry loop confirming the subscription is registered) instead of relying purely on wall-clock timing; document the accepted flake budget in validation.md. | P2 |
| 5 | `SessionList.tsx` mounting `useInsightsSummary()` (Task 4.1.1a) adds a second persistent, auto-reconnecting `WatchInsights` stream per open browser tab (confirmed: the hook holds an `AbortController`-scoped reconnect loop, `useInsightsService.ts:87-157`) — turning what was an occasional dashboard-only subscription into an always-on stream for the app's likely-default view. The plan's Risk Control table dismisses this using only `GetInsightsSummary`'s cheap in-memory cost, not the streaming subscription's steady-state goroutine/channel cost. | Gradual goroutine/memory growth (visible via existing pprof tooling) correlating with number of open stapler-squad tabs/worktrees — invisible in single-tab dev testing, noticeable only after real multi-session usage accumulates. | Run a load check with N simulated concurrent `SessionList` mounts before ship (per `.claude/docs/profiling.md`), or implement Unresolved Question #2's deferred gating ("subscribe only after Sort: Cost is selected") as an actual task instead of a follow-up. | P3 |

## P1 Items (address before implementation)
- [x] #3 — Replace AC-5's `coverage-gaps.json` diff verification step (Tasks 1.2.1d, 5.1.1c) with a check against an artifact that actually reflects the per-feature JSON directory (`registry-aggregate`'s `docs/registry/frontend-features.json`, or `make registry-diff`), or add real `// +feature:` markers to the 5 target source files so the marker-driven scanner path also picks them up. **Resolved 2026-08-02**: plan.md Tasks 1.2.1d/5.1.1c and Epic 1.2/5.1's Given/when/then rewritten to grep the 5 new ids directly out of `docs/registry/frontend-features.json` post-`registry-aggregate`, dropping `coverage-gaps.json` as the proof (confirmed it's structurally insensitive to this directory — all 5 target files already share the matched `insights-dashboard` marker, so its `unmatchedFrontend` count is 0 regardless of these files' content).

## P2 Items addressed proactively (not required by the gate, fixed because cheap)
- [x] #2 — Task 3.2.3b's badge composition snippet corrected to `[badge, badgeVariant.warning].join(" ")` (was `badgeVariant.warning` alone), matching `TokenBadge.tsx:47`'s real composition.
