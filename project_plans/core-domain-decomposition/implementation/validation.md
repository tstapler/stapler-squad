# Validation — Core Domain Decomposition

## Test Coverage Mapped to Requirements

| Requirement | Test | Type |
|---|---|---|
| R1.1 (`CheckpointService` extraction, no behavior change) | Existing checkpoint tests relocated + re-run unmodified against the new service; `go test -race ./server/services/...` | Unit/integration, regression |
| R1.2 (`AutonomousOrchestrationService` extraction) | Existing autonomous-driver tests relocated + re-run; manual UI pass starting/stopping autonomous mode | Unit/integration + manual |
| R1.3 (`GetProviderLimits`/`GetDetectionEvents` evaluation) | N/A — investigation task, not a code change unless a decision is made to extract | N/A |
| R1.4 (setter/getter audit) | `go build ./...` after each incremental conversion; existing tests exercising `server/dependencies.go`'s builder stages | Build + existing regression |
| R1.5 (no RPC contract change, Epic 1) | Full `tests/e2e/` suite unmodified against decomposed services | E2E |
| R1.6 (new services follow the established constructor/field/delegate shape) | Code review against `project_service.go`/`github_service.go`'s existing pattern during Tasks 1.1a/1.2a/1.4a/1.4b/1.5a/1.6a; `go vet`/`golangci-lint` pass (no structural pattern deviation) | Code review + lint |
| R1.7 (colocated test file per extracted service) | Task 1.1b (`checkpoint_service_test.go`), Task 1.2e (`autonomous_orchestration_service_test.go`, relocating/extending `autonomous_integration_test.go` — verified to actually cover the moved methods, not assumed), Task 1.5b (`terminal_service_test.go`), Task 1.6b (`feature_flag_service_test.go`); `workspace_service_test.go`/`workflow_service_test.go` additions for R1.9/R1.10 (existing files, new coverage) | Unit/integration |
| R1.8 (governance ADR for the cluster-boundary convention + new services) | Already satisfied — `decisions/ADR-001` documents the standing rule; `decisions/ADR-002` (scope-corrected 2026-07-01) documents this project's specific new services (`CheckpointService`/`AutonomousOrchestrationService`/`TerminalService`/`FeatureFlagService`) plus the `ListBranches`/`ArchiveWorkflowSessions`/`DeleteWorkflowFailedSessions` delegations to existing services | N/A (documentation) |
| R1.9 (`ListBranches` → `workspaceSvc` delegation) | Existing `ListBranches` tests relocated to `workspace_service_test.go` + re-run unmodified; `go build ./... && make test`; manual branch-dropdown exercise (Task 1.4c) | Unit/integration, regression + manual |
| R1.10 (`ArchiveWorkflowSessions`/`DeleteWorkflowFailedSessions` → `workflowSvc` delegation) | Existing workflow-archival/cleanup tests relocated to `workflow_service_test.go` + re-run unmodified; `go build ./... && make test`; manual workflow session archival/cleanup exercise (Task 1.4c) | Unit/integration, regression + manual |
| R1.11 (new `TerminalService` for `GetTerminalSnapshot`/`WriteToSession`) | New `terminal_service_test.go` (Task 1.5b) covering both methods and the shared instance-lookup fallback helper, including its not-found and controller-vs-fallback branches; manual terminal snapshot fetch + `WriteToSession` keystroke exercise (Task 1.5c) | Unit/integration + manual |
| R1.12 (new `FeatureFlagService` for `GetFeatureFlags`/`UpdateFeatureFlag`) | New `feature_flag_service_test.go` (Task 1.6b) covering known-flag validation, config persistence, and `FeatureController` toggling; manual feature-flag toggle exercise through the UI (Task 1.6c) | Unit/integration + manual |
| R2.1/R2.2 (tmux normalization at `Preview()`, corrected both-branches design) | New tests on **both** of `Preview()`'s branches confirming normalized (not raw) output (Task 2.1c): one exercising the controller-attached path (mock/stub `ClaudeController` returning ANSI-laden bytes via `GetRecentOutput`), and one exercising the `CapturePaneContent()` fallback path, using this repo's existing mock-tmux test infrastructure (`buildWithMockTmux`). The controller-branch test is the one that closes the actual gap the adversarial review found — it must not be skipped in favor of only re-testing the already-covered fallback branch. Existing `normalizer_test.go` coverage of the normalization logic itself is unchanged. | Unit |
| R2.3 (`classifier.go` non-finding) | One-time scoped `git log` re-check (Task 2.2a) — not a code test, a verification step | Manual/investigative |
| R2.4 (no behavior change to approval-rule classification outcomes) | Existing `approval_handler.go`/classifier tests re-run unmodified against the normalized `Preview()` output; manual approval-rule pass exercising autonomous-mode approval (the call path that exercises the primary/controller branch) to confirm classification outcomes are unchanged | Regression + manual |
| R3.1-R3.3 (governance ADRs) | Already satisfied — `decisions/ADR-001/002/003` written during planning | N/A (documentation) |

## Implementation Readiness Gate

1. **Requirements traceable to tasks**: R1.1-R1.12, R2.1-R2.4, R3.1-R3.3 all map to at least one task in `plan.md` — confirmed above. ✅
2. **No unresolved blocking ambiguity**: Open Decisions table in `plan.md` has 3 rows, all explicitly deferred to implementation-time verification (not blocking planning) or already resolved by sequencing rules. ✅
3. **Sequencing conflicts identified and addressed**: Epic 3 explicitly blocked on `instance-actor-concurrency`'s Epic 1; Epic 1/2 here explicitly unblocked and parallel-safe. Task 1.2b explicitly does NOT attempt to fix the concurrency race it touches, deferring to the other project by design, not oversight. ✅
4. **Scope corrected against actual code, not assumption**: the original hypothesis (session_service.go as an undecomposed monolith) was disproven by direct inspection during planning and the requirements/plan were rewritten to match reality (26 existing services found; scope is now 4 new-service extractions — `CheckpointService`, `AutonomousOrchestrationService`, `TerminalService`, `FeatureFlagService` — plus 2 delegations to already-existing sibling services — `ListBranches`→`workspaceSvc`, `ArchiveWorkflowSessions`/`DeleteWorkflowFailedSessions`→`workflowSvc` — plus 1 normalization fix (both `Preview()` branches) plus 1 setter/getter audit task; the R1.9-R1.12 additions were found by the first adversarial review, not the original planning pass) — this is a stronger position than planning against an unverified assumption would have produced. ✅

**Verdict: PASS.** Ready for implementation. Recommend starting with Epic 2 (smallest, most isolated — one method, `Preview()`, normalized once at its single return point downstream of both its branches — see the corrected Task 2.1c design) before Epic 1 (now six extraction targets total: `CheckpointService`/`AutonomousOrchestrationService` (original scope) plus `ListBranches`/`ArchiveWorkflowSessions`/`DeleteWorkflowFailedSessions` delegations and `TerminalService`/`FeatureFlagService` (R1.9-R1.12, added by the first adversarial review)) to validate the pattern with minimal risk first.

## Self-Adversarial Review

(Performed directly, in place of a separate reviewer agent — this plan was produced by a single planning pass, not the multi-agent parallel research/synthesis/adversarial-review pipeline used for `instance-actor-concurrency`, since this instance of planning ran as a single agent without sub-agent dispatch capability. Treat this plan as **less adversarially tested** than that project's — a follow-up adversarial pass by a fresh reviewer, ideally with sub-agent dispatch available, is recommended before Epic 1/2 implementation begins, even though the readiness gate above passes on its own terms.)

Findings from re-reading this plan critically:

1. **Verified, not assumed**: the "26 existing services" and "6-line delegate" claims are backed by direct `grep`/body-line-count reads performed during this planning pass (see `research/architecture.md` §1-2), not inference from file names alone. This is the plan's strongest point — it caught and corrected its own initial wrong assumption (that `session_service.go` was undecomposed) before committing to a large, unnecessary rewrite.
2. **Weakest point**: `Task 1.2a`'s `wireXCallback` consolidation decision and `Task 1.1b`'s test-file-location assumption are both explicitly deferred to implementation-time verification rather than resolved now — this is honest about the limits of a single research pass without a dedicated agent to trace every call site, but means the plan carries residual unknowns a fresh implementer must resolve, not a fully closed specification.
3. **Not independently verified**: this plan's own claims were not checked by a second, independent reviewer (unlike `instance-actor-concurrency`, which went through 4 adversarial review rounds and caught real blockers each time). The specific risk pattern that process caught repeatedly — a design that looks correct on paper but has an unverified assumption buried in it — could equally apply here and has not been stress-tested the same way. Recommend a genuine second-pass review (a fresh Task/Agent dispatch, not a continuation of this same planning pass) before starting Epic 1's implementation, given how much value that pattern demonstrably added on the sibling project.
