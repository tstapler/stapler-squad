# Adversarial Review: backlog-configurable-pipeline

**Date**: 2026-07-15 (re-check of fix pass)
**Verdict**: CONCERNS

Re-check scope: this pass verifies only whether the 4 prior BLOCKER findings were actually resolved in the updated `plan.md`, plus a spot-check of 2 of the 6 prior CONCERNS that were folded into the same fix pass. It does not re-review the rest of the plan. All 4 blockers are RESOLVED. Both spot-checked concerns are RESOLVED. No new blocker-level issue was introduced by the fix pass. The remaining (not re-examined) concerns and minors are carried forward unchanged below, so Verdict moves from BLOCKED to CONCERNS, not CLEAN.

## Blockers (re-check results — all RESOLVED)

- [x] **RESOLVED — `pipelineModeCache.Invalidate` writer-side serialization.** Verified in Story 1.3.2 (plan.md lines 218–225) and its tasks:
  - The AC (line 221) states explicitly: `pipelineModeCache` wraps `atomic.Pointer[...]` plus an unexported `sync.Mutex` (`writeMu`) that guards **ONLY the write path**; `Get(slug)` "does a single atomic `Load()` + map lookup with no locking whatsoever — it never touches `writeMu`." This is the correct shape (write-path-only mutex, lock-free reads) — matches the claimed fix exactly.
  - Task 1.3.2b (line 231–233) implements `Invalidate` with `c.writeMu.Lock(); defer c.writeMu.Unlock()` around the re-fetch+Store sequence, and updates `Load` to share the same mutex.
  - A concrete concurrent-`Invalidate`-race test now exists as its own task, not just restated intention: Task 1.3.2d (lines 239–241), "Lost-update concurrency test for concurrent `Invalidate` calls" — N=10 goroutines each calling `Invalidate` with version-tagged, latency-varied fake repository responses, asserting the final cache state reflects whichever call *began* its DB read last, not whichever `Store` happened to land last. This is explicitly distinguished from the separate torn-read test in Task 1.3.2c (line 236, "no data race... holds a `Get()` result across a concurrent `Invalidate()`"). The AC (line 224) also states this third Given-When-Then scenario directly. Also cross-referenced in the Pattern Decisions table (line 54) and the Domain Glossary's `pipelineModeCache` entry (line 27).

- [x] **RESOLVED — placeholder allow-list validation, with matching allow-lists across phases.** Three claims verified:
  1. **Story 2.3.1's AC now specifies real write-time allow-list rejection**: lines 638–641. The AC text explicitly states scanning all 9 content-template fields for `{{...}}` tokens and rejecting the request if any token is not in the recognized allow-list, with three concrete Given-When-Then cases (invalid slug rejected, unrecognized-placeholder rejected naming the field+token, all-recognized-placeholders accepted). Task 2.3.1a/c implement and test this.
  2. **Unresolved Questions entry is marked resolved**: line 91, `[x] ~~...~~ **RESOLVED — write-time allow-list rejection, implemented in Story 2.3.1.**`
  3. **Task 1.3.3d's passthrough is explicitly framed as Phase-1-temporary**: line 275, "this is a TEMPORARY, PHASE-1-ONLY passthrough ... explicitly superseded once Story 2.3.1 ships in Phase 2 ... this passthrough branch becomes unreachable in practice rather than being the permanent behavior." Also stated in the Domain Glossary's `renderTemplate` entry (line 33) and the Unresolved Questions resolution note (line 91).
  4. **Allow-list consistency check (the specific new-inconsistency risk flagged for this re-check)**: Task 1.3.3d (line 275) lists the `renderTemplate` allow-list as `item_id, item_title, item_description, criteria_index, criteria_count, criteria_text, repo_path`. Story 2.3.1 (line 638) lists the `ValidatePipelineModeContent` allow-list as the identical 7 names in the identical order, and says so explicitly ("the same list `renderTemplate` in Task 1.3.3d recognizes"). The Domain Glossary's `renderTemplate` row (line 33) states the allow-list is "shared by `renderTemplate` and `ValidatePipelineModeContent`." No divergence found — Phase 1 and Phase 2 use the same list A = list B.

- [x] **RESOLVED — Task 1.4.4c no longer references a nonexistent precedent test.** Verified at lines 359–361: the task now directly names the two Given-When-Then cases from Story 1.4.4's own AC (omitted-field-preserves-existing-value; explicit-empty-resets-to-default) and instructs "write these two cases directly from the acceptance criteria instead of searching for one." It retains a factual note that no precedent test exists (still true, still correctly grep-verified: `clobber`/`currentFlags` absent from test files) but this is now backward-looking context, not an instruction to search — the prior "go find and mirror it" framing is gone.

- [x] **RESOLVED — `NewPipelineEngine` startup-failure behavior documented in both locations, test asserts success.** Verified:
  - Story 1.3.3's AC (lines 252–253) specifies graceful degradation explicitly: on `cache.Load` failure, "`NewPipelineEngine` does NOT return that error to its caller — it logs [Warn] ... and returns a valid `*CachingPipelineEngine` backed by an empty (zero-mode) cache. `NewPipelineEngine`'s signature stays `(repo) (*CachingPipelineEngine, error)`... but the Phase 1 implementation never actually returns a non-nil error for a `cache.Load` failure."
  - Risk Control section (line 87) has a dedicated "`NewPipelineEngine` startup-failure behavior" paragraph with identical content, plus the operational rationale ("a transient DB hiccup at boot must never crash the whole server").
  - Task 1.3.3f (lines 269–271), "Test `NewPipelineEngine`'s graceful-degradation-on-`cache.Load`-failure behavior," asserts construction returns `(non-nil engine, nil error)` when `ListEnabled` errors — confirmed this is a construction-**succeeds** assertion, not a construction-fails one. The AC's own Given-When-Then (line 253) states the same: "`NewPipelineEngine(repo)` is called, *Then* it returns `(engine, nil)` where `engine` is non-nil and usable."

## Concerns

Two of the six prior concerns were spot-checked this round; both RESOLVED. The remaining four were not re-examined this pass and are carried forward unchanged from the previous review round.

- [x] **RESOLVED (spot-checked this round) — Cache-invalidation failure after a successful DB write.** Story 2.2.1's AC (lines 581–582) now has an explicit acceptance criterion: if `Create`/`Update` succeeds but the subsequent `InvalidateCache` call fails, "the handler still returns success to the caller ... failing the RPC over a read-side cache-refresh hiccup would be a confusing, inaccurate error," with a `[PipelineEngine]` Warn log noting the row may be stale until the next successful invalidation. A Given-When-Then case confirms RPC-still-succeeds behavior. Task 2.2.1d adds a dedicated test for exactly this scenario.

- [x] **RESOLVED (spot-checked this round) — No appetite/scope-cut check-in.** The Risk Control section now has an explicit "Appetite / scope-cut check-in" paragraph (line 88) stating: "this plan ships the full CRUD management UI in Phase 3 ... not a minimal/CLI-first cut, even though requirements.md's Appetite section pre-authorizes descoping it ... This is a deliberate choice, recorded here rather than left implicit," and explains the fallback (descope to minimal form/seed-script at Phase 3 start if budget is tight, without touching Phase 1/2 work). Phase 3's own header (line 659) cross-references this same paragraph via a "Scope note."

- [ ] **NOT RE-EXAMINED (unchanged from previous round) — Bootstrap-ordering claims in Epic 1.5 traced by line-number reference.** Note for context only, not verified to this round's standard: current plan.md's Story 1.5.1 AC (lines 381–383) now contains a substantially expanded bootstrap-ordering trace (citing `server/dependencies.go:296`, `:444`, `:483`, `:871`, and directly addressing why `WorkflowEngine` is not valid precedent) that appears responsive to this concern's substance, but this pass did not independently verify those line numbers against the actual source file, so the concern is left open/unchecked rather than marked resolved.

- [ ] **NOT RE-EXAMINED (unchanged from previous round) — The 4-method `PipelineEngine` interface is consumed in full by callers that use only one method.** Note for context only: the interface is now 5 methods (`ContentHashFor` was added, per the Pattern Decisions table's "method count" row, lines 38 and 57), with an explicit justification for not splitting it further. Not independently re-verified this pass.

- [ ] **NOT RE-EXAMINED (unchanged from previous round) — `research/features.md` §4's "prospective preview" recommendation.** Note for context only: current plan.md now has an "Out of Scope (plan-level...)" section (lines 68–69) stating the prospective-preview recommendation "was considered and explicitly deferred, not overlooked," which appears responsive, but this was not independently re-verified against `research/features.md` this pass.

- [ ] **NOT RE-EXAMINED (unchanged from previous round) — The "single shared `PipelineEngine` instance" claim in Story 1.5.1 had no corresponding task.** Note for context only: Task 1.5.1e (lines 402–404) now exists, "Pointer-equality integration test for the single shared `PipelineEngine` instance," explicitly citing this concern by name. Appears responsive but not independently re-verified this pass.

## Minors

*(unchanged from previous round — not re-examined this pass)*

- `Task 1.6.2b` (snapshot the resolved mode on `TriggerTriage`'s session-creation path) doesn't address what happens if a headless-triage run never produces a full `ItemSession`/work session — is `PipelineModeSnapshot` simply never written in that case, and if so, is that distinguishable from "default mode was used"? Low-impact ambiguity, cheap to clarify.
- Task 1.3.4b is visibly undecided in its own text ("OR do both in the same task if under the 3-5 file cap... given exactly 3 files are touched, do both here") — harmless, but signals the plan wasn't fully settled when written.
- `DeletePipelineMode` (Story 2.2.2) allows deleting a mode with live references and relies entirely on fail-closed resolution + Warn logs for operator awareness after the fact. A simple "referenced by N items" count in the delete-confirm dialog (Story 3.3.2) would cheaply reduce the self-inflicted-breakage risk the NFR section is centrally about, since the frontend already has the item list in memory.
- The Domain Glossary and Pattern Decisions tables are unusually thorough and well cross-referenced to research artifacts — a genuine strength that makes the gaps above stand out by contrast, since the plan clearly *can* trace things carefully (e.g. `research/architecture.md` §2's autonomous-driver goal-chain trace) when it chooses to.
