# Adversarial Review: plan-feedback

**Date**: 2026-09-14
**Verdict**: CONCERNS

## Blockers

None. All three prior blockers are resolved:

- ~~`handleSendBackWithFeedback`'s catch block never calls `load()`~~ — **Resolved.** Task 2.2.1a's catch block (plan.md:599-613) now calls `await load();` unconditionally, before the error toast and rethrow, with a comment explaining why it's unconditional (avoids a stale `item.status`/`item.updatedAtRaw` poisoning a retry's CAS precondition). Story 2.2.1's acceptance criteria (plan.md:543-563) are now split into two explicit Given-When-Then blocks — call-3 (`triggerTriage`) fails, and call-2 (`rejectPlan`) fails — both asserting `load()` runs before the error is shown.
- ~~InlineNotice's "will not be interrupted" claim is false~~ — **Resolved.** Copy changed everywhere to "submitting this will stop it, and its work may be superseded once re-planning completes.", consistent across Story 2.1.1's AC (plan.md:343-347), Task 2.1.1b's code sample (plan.md:458), and `design/ux.md` §3's wireframe (ux.md:79-80) and §7's error-state discussion. Grepped both files for the old "will not be interrupted" string — zero matches remain.
- ~~No test exercises `ErrVerdictClearRequiredForReady` with a real PASS verdict~~ — **Resolved.** Task 1.2.1c (plan.md:264-305) now specifies `TestTransitionBacklogItemStatus_should_StopLiveWorkSession_When_PassVerdictPresentAndOverrideReasonSet`, which creates a real PASS verdict via `storage.CreateItemSessionWithVerdict(..., session.ReviewVerdictData{OverallOutcome: session.ReviewVerdictPass})` and asserts the transition succeeds with `OverrideReason` set and the live session is torn down. The old "omit the verdict setup entirely" instruction is gone (grepped, zero matches); the task now explicitly says "do **not** omit this setup, it's the scenario the plan actually needs proven." Verified the plan's claim that the complementary "fails without override reason" case is pre-existing coverage: `TestTransitionBacklogItemStatus_should_ReturnFailedPrecondition_When_ReviewToReadyBlockedByPassVerdict` is real, at `server/services/backlog_service_lifecycle_test.go:449`, and asserts `connect.CodeFailedPrecondition` when no `OverrideReason` is supplied against a PASS-verdict item; `TestTransitionBacklogItemStatus_should_Allow_When_ReviewToReadyHasOverrideReason` is real, at `:489`, and asserts success with `OverrideReason` set (though it doesn't assert teardown — that's exactly the gap the new test fills, matching the plan's stated division of labor).

## Concerns

- [ ] **The teardown failure path still has zero coverage.** Checked `server/services/backlog_service_test.go`'s current `mockSessionStopper` (lines ~200-262): `StopSessionByUUID` is still `func (m *mockSessionStopper) StopSessionByUUID(_ context.Context, _ string) error { return nil }` with no error-injection field (no `stopperErr`, contrast with the existing `archiveErrForUUID` map on the same struct). Task 1.2.1b only adds a `stoppedUUIDs []string` field to record successful calls, not a way to make the call fail. No new task anywhere adds error injection or asserts the transition still completes when teardown fails. This is the exact gap the prior review flagged — the fix subagent's summary doesn't claim to have addressed it, and it hasn't been. — **Recommendation**: add a `stopperErr`/similar field to `mockSessionStopper`, and one test asserting the transition still completes (and returns success) when `StopSessionByUUID` or `UpdateItemSessionEnded` fails, per the "best-effort, non-fatal" design the Observability Plan describes.

## Minors

- `RejectPlan` itself does a plain `Get()`-then-`Update()` with no CAS/version precondition (`backlog_service_lifecycle.go:915-936`) — the same TOCTOU shape BUG-026 fixed for `TransitionBacklogItemStatus`. Pre-existing, not introduced by this plan, but the new 3-call sequence stacks an unprotected write immediately after a protected one, marginally widening the window. Not blocking.
- Old `TriageResult` rows: each retrigger calls `CreateItemSession` (verified at `server/services/backlog_service_trigger_triage.go:321`) rather than overwriting the prior triage `ItemSession` row, so repeated send-backs accumulate history rows over an item's lifetime (`findPriorTriageResult` just reads the most recent one). This is pre-existing behavior for every triage run, not specific to this feature, and the on-disk `plan.md` is overwritten in place per pitfalls.md §2 (no file-level orphan). The plan doesn't mention this, but it isn't a new risk introduced by this change — worth a one-line acknowledgment only.
- The plan's single "Unresolved Question" (whether `send_back_idea` should get the same teardown fix) is genuinely non-blocking as written — it's scoped to a different action (`send_back_idea`), correctly deferred to a follow-up item, and doesn't gate any story scheduled in this plan.
- Scope check: no drift found. The plan stays within requirements.md's In Scope list and correctly leaves `steer_session` and multi-operator concurrency untouched; the `send_back_idea` block is explicitly left alone (Task 2.2.1c).

## Resolved-but-worth-noting

- The prior Concern about the Observability Plan contradicting the implementation code (claiming `log.WarningLog()` while the code did bare `_ =` discards) is now resolved as a side effect of other edits: Task 1.1.1a's `stopLiveWorkSessions` code (plan.md:150-172) now calls `log.WarningLog().Printf(...)` on all three failure paths (list-sessions, stop-session, mark-ended), matching the Observability Plan's claim exactly.
- The prior Concern about the undiscussed `rejectPlan`-specific mid-sequence failure is now substantively addressed: Story 2.2.1's AC explicitly adds a second Given-When-Then block for "call 2 (`rejectPlan`) rejects," and `design/ux.md` §7 adds a table mapping each failure point to the resulting item state and recovery affordance, plus a named residual-gap note about the copy being slightly less precise for this specific sub-case (not blocking — the headline stays accurate). No dedicated new unit test was added specifically distinguishing call-2-fails from call-3-fails, but the catch-block code path is identical for both (unconditional `load()`), so one is a reasonable proxy for the other's correctness.
