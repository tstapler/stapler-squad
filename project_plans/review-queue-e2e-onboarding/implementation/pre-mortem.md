# Pre-mortem: review-queue-e2e-onboarding
**Date**: 2026-08-03

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | Base branch is local `main` (confirmed 26 commits behind `origin/main`), so `tests/e2e/escalation-reasoning.spec.ts` doesn't exist locally and Task 1.1.1e can't run — verified: `git show main:tests/e2e/escalation-reasoning.spec.ts` fails, `git show origin/main:...` succeeds (241 lines, added by PR #315). `review-queue.spec.ts` itself is byte-identical between the two bases, so only the migration task is blocked, not the core fix. | `git show origin/main:tests/e2e/escalation-reasoning.spec.ts` (Task 1.1.1e's own sanity check) fails, or the implementer silently skips 1.1.1e and ships a PR that violates AC4. | Add an explicit pre-Task-1.1.1a gate to plan.md: "rebase/checkout worktree onto `origin/main` before starting" — not a footnote buried in the Domain Glossary. | P1 |
| 2 | Acceptance-criteria arithmetic doesn't add up. Direct count of `origin/main:tests/e2e/review-queue.spec.ts`: 11 `test()` bodies (3 smoke + 2 session-creation + 3 acknowledge-flow + 3 permanently-skipped via `test.describe.skip`) × 2 unrestricted projects (`chromium`, `chromium-dom` — neither has a `testMatch` filter excluding this file) = 22 tests per full run, not the "18 total = 15 passed + 3 skipped" that requirements.md AC2 and plan.md Story 1.1.2 both assert. | `npx playwright test review-queue.spec.ts --reporter=list` reports 22 (or a project-scoped 11), and the implementer either flags the plan as wrong, stalls, or quietly fudges the verification writeup to claim "18 passed" without it being true. | Replace the fixed literal count in requirements.md AC2 / plan.md Story 1.1.2 with a formula (0 failed; skipped = 3-per-project always-skipped block + any runtime `test.skip()` triggered inside the acknowledge test; remainder passed) — verify the formula once via a `--list` dry run before implementation starts, not during it. | P1 |
| 3 | The "proven pattern" being copied doesn't match the plan's literal call-site instruction. In `origin/main`'s `escalation-reasoning.spec.ts`, the dismissal click sits *after* `waitForSelector('[data-testid="review-queue-loaded"]', {state:'attached'})`, not before it as Tasks 1.1.1b–1.1.1d instruct ("immediately after `page.goto(...)` and before the first subsequent `waitForSelector`"). `state:'attached'` waits don't care about an overlay on top, so this is likely harmless — but any `review-queue.spec.ts` test whose first wait is a default-state (`visible`) `waitForSelector`/`toBeVisible()` right after `goto()` reintroduces exactly the ~800ms modal-mount race the fix exists to close, just relocated. | Suite passes locally on a fast/idle machine but a specific subset (the tests with the shortest wait window right after `goto()`) fails intermittently under CI, not the clean "10/18 always fail" signature from triage. | Task 1.1.2a/b should record *which* wait/assertion each test hits first and confirm the dismissal call precedes it in wall-clock terms, not just textually — don't trust "same pattern as escalation-reasoning.spec.ts" without diffing the actual surrounding wait calls, since the two files' post-goto wait shapes differ. | P2 |
| 4 | CI's real failure signature (from the original backlog bug report) differs from what local triage reproduced — e.g., CI fails on a different selector/timeout than the 10 catalogued combinations, or only under resource contention that a deterministic `--workers=1` local repro doesn't exercise. The fix closes the reproduced signature but leaves a CI-only failure mode open. | PR's actual CI run for `review-queue.spec.ts` still shows red (or a different subset of failures) despite a clean local pass in Task 1.1.2a/b. | Plan's verification step should include one run against the real CI runner (or closest available approximation), not only a local machine, before Story 1.1.2 is marked complete; link the original failing CI run in the PR for side-by-side comparison. | P2 |
| 5 | Fix passes in CI at merge time, then flakiness returns within the first month under real parallel-job/CI-load contention — the fixed 5000ms click-timeout budget was sized against an uncontended local run, and a busy CI queue can push the modal's ~800ms mount time (plus general DOM-settle slowdown) close enough to the budget that the original timeout symptom reappears intermittently instead of deterministically. | A new (or reopened) bug citing intermittent `review-queue.spec.ts` failures weeks later, now flaky rather than the original reliably-reproducible 10/18 — harder to triage because "fixed" status makes it look unrelated. | validation.md should call for at least one deliberate "loaded" verification run (concurrently with other CI jobs, or CPU-throttled) in addition to the clean isolated run, and should note the 5000ms budget as a known tunable if flakiness resurfaces. | P3 |

## P1 Items (address before implementation)
- [x] #1 — RESOLVED: added Task 1.1.0 (prerequisite, runs before Task 1.1.1a) to
  plan.md requiring `git show origin/main:tests/e2e/escalation-reasoning.spec.ts`
  to succeed before proceeding, plus a corresponding node in the Dependency
  Visualization diagram.
- [x] #2 — RESOLVED: replaced the "18 total = 15 passed + 3 skipped" claim with
  a "0 failed across 22 test instances (11 test bodies × 2 projects)" formula
  in requirements.md AC2, plan.md Story 1.1.2 + Tasks 1.1.2a/b, and
  validation.md's Requirement→Test Mapping + Coverage Targets sections. Task
  1.1.0 also directs the implementer to confirm the count once via
  `--list` before starting.
