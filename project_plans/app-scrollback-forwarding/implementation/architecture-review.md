# Architecture Review: app-scrollback-forwarding (iteration 3 — re-review of previously blocked/concern items)
**Date**: 2026-09-14
**Verdict**: CLEAN

## Re-reviewed items
- [x] Blocker (mobile touch trigger) — RESOLVED — Task 1.4.0c (plan.md:966-1002) now extends `UseTerminalGesturesOptions` (plan cites `useTerminalGestures.ts:30-37`) with optional `isAltScreenActive?: () => boolean` and `onAltScreenScrollUp?: (lines: number) => void`, threaded via the same stable-ref pattern as `onSendData`/`longPressMs` (`onSendDataRef`/`longPressMsRef`, cited at `useTerminalGestures.ts:51-60`), and branches inside the existing `SCROLLING` state's throttled callback (cited at `useTerminalGestures.ts:240-247`) when `lines < 0`. Read the real file: the 5-state machine (`IDLE → PENDING → SCROLLING | SELECTING | TAPPING → IDLE`, lines 6-7/28), the `UseTerminalGesturesOptions` interface at lines 30-37, the ref-mirroring `useEffect`s at lines 51-60, the 15px `PENDING`→`SCROLLING` threshold at line 228, and the `lines` computation inside the throttled callback at lines 240-247 (`if (lines !== 0) terminal.scrollLines(lines)`) all match the plan's citations exactly — no code has been touched yet (expected; this is still the Phase-3 plan artifact, not an implementation), but every line the plan hangs its design on is real and correctly described. The insertion point the plan proposes (branch before the existing `scrollLines(lines)` call) sits inside the one state (`SCROLLING`) that's already mutually exclusive with `SELECTING`/`TAPPING`, so it adds a second *outcome* to the existing recognizer instead of a second listener — the original conflict (an independent touchstart/touchmove pair racing `useTerminalGestures`'s own document-level listeners, the exact R4.3/ADR-012 bug the hook's header comment describes) is designed out, not just described away.
- [x] Concern (AppScrollGate legacy path) — RESOLVED — plan.md:168-178, Task 1.1.3a (plan.md:505-511), and Task 1.3.3b (plan.md:806-812) now all explicitly state that `streamViaControlMode`'s closure passes a fixed `subscriberCount: -1` sentinel rather than deriving a count from `activeControlModeStreams`, and cite why: `activeControlModeStreams`'s own doc comment (`server/services/connectrpc_websocket.go:320-331`, verified by reading it directly) says it is "Observability only (no behavior change): tracks which streamViaControlMode generation currently 'owns' a given tmux session name" — i.e. stream-*generation* bookkeeping for log correlation, not a concurrent-viewer count, and its own comment notes two overlapping generations can run against the same session at once. Since `AppScrollGate`'s check (4) is `subscriberCount == 1` (Task 1.1.3a), `-1` deterministically fails it, producing `Blocked` for every `PathLegacyPerConnection` call — the same outcome the plan previously asserted without stating a mechanism. The current (unimplemented) `onScrollbackRequest` closure at `connectrpc_websocket.go:1342` still has the old `func(startLine, endLine string) (string, error)` signature, confirming this widening is correctly scoped as not-yet-done work rather than a claim about code that already changed.

## Blockers
(none)

## Concerns
(none)

## Nitpicks
- Task 1.4.0c's directional convention (`lines < 0` ⇒ alt-screen scroll-up path) is internally consistent with Task 1.4.0b's desktop `deltaY < 0` check and with the sign arithmetic already in the `SCROLLING` callback (`lines = Math.round(-moveDy / cachedCellH)`), but the plan never states the derivation in one place — a future reader verifying "does drag-down really mean scroll-up" has to work it out from the raw formula rather than from a sentence in the plan. Not blocking; worth a one-line note if Task 1.4.0d's test descriptions don't already make it self-evident.
