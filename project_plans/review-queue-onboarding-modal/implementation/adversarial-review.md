# Adversarial Review: review-queue-onboarding-modal

**Date**: 2026-08-15
**Verdict**: CONCERNS (all 1 blocker + 5 concerns resolved directly in plan.md this pass — see `[x]` items below; minors not yet actioned, low-consequence)

## Blockers

- [x] **No contingency if Task 1.1.1a's re-run doesn't reproduce 0 failed/14 passed/8 skipped.** Every "pass" disposition in Story 1.1.1, and the whole "keep the fix, fail only AC4" strategy, rests on `npx playwright test review-queue.spec.ts --reporter=list --retries=0` reproducing exactly the count requirements.md already claims. The Given/When/Then for AC2/AC3 implicitly gates on that number ("it must report 0 failed... before `report_progress(...)` is called"), but nothing in Task 1.1.1a/1.1.1b says what to do if the live re-run comes back different — e.g. a new flake, an environment drift since the 2026-08-03 implementation, or a regression from `chore(sdd)` commits (`cb82049a5`, `85ddf26e9`) layered on top since. Add an explicit branch: if the re-run doesn't match, stop, do not mark AC2/AC3 pass, and re-open root-cause investigation (do not silently accept a different number, and do not silently keep citing the stale 2026-08-15-morning numbers from requirements.md as if they were this task's own evidence).
  - **Resolved**: plan.md's Task 1.1.1a now has an explicit "Contingency" paragraph — stop, don't mark AC2/AC3/AC7 pass, re-open root-cause investigation if the re-run doesn't match.

## Concerns

- [x] **Backlog item's own prior verdict text is internally inconsistent about which AC actually failed, and the plan doesn't reconcile it.** `get_backlog_item(5ab8a12e-17a3-441c-bc4c-88be826eb5bf)`'s "Latest Review Verdict" says "AC3 fails" and separately "Criterion 2 (FAIL)," but the cited reasoning in both places ("modified `ReviewQueuePanel.tsx`... rewrote assertions/selectors of two tests... explicitly prohibited by AC3's literal text") is verbatim AC4's text (item AC #4: "no assertion, selector, or test intent is modified... no file outside `tests/e2e/`... is touched"), not AC3's ("A re-run with `--retries=0` also reports 0 failed"). "AC3" (1-indexed) and "Criterion 2" (0-indexed) are mutually consistent with each other but both point one index short of AC4/`criteria_index=3`. The plan's blanket re-evidencing of all 7 indices this pass neutralizes the practical risk (every index gets a fresh, correctly-cited disposition regardless of what the prior verdict actually recorded), but the plan never notices or flags the discrepancy. Recommend one sentence in `request_review`'s `message` reconciling it, so a human reviewer whose own mental model says "I failed AC3" isn't confused by this pass's "AC4 fail."
- [x] **Plan never mentions the `sdd:6-verify` gate `.claude/commands/backlog/review.md` requires before calling `request_review`.**
  - **Resolved**: plan.md's Story 1.2.1 now has an explicit "Before calling request_review, run sdd:6-verify" bullet citing the prior 2026-08-03 CLEAN artifacts and requiring a fresh run this pass. That doc's first instruction: "Before requesting review, run the `sdd:6-verify` skill... Resolve every BLOCKER and REFACTOR finding it surfaces. Only continue once it reports PASS, or you have deliberately accepted and documented any remaining CONCERNS." Plan.md cites this same file for the 3-cycle rule (Task 1.2.1b) but is silent on this earlier, mandatory step. In fact the gate does appear to have been satisfied historically — `project_plans/review-queue-e2e-onboarding/implementation/architecture-review.md` and `adversarial-review.md` (both dated 2026-08-03, both verdict CLEAN) cover the exact diff still on `HEAD` — but the plan doesn't cite this, so a reader of plan.md alone has no way to know the gate was honored rather than silently skipped. Add a one-line citation of those two files as satisfying the gate for an unchanged diff.
- [x] **AC7's evidence plan doesn't establish an actual "pre-migration" baseline to compare against.**
  - **Resolved**: plan.md's Task 1.1.1a now instructs the `git diff 303d43fad:... HEAD:...` baseline check as part of AC7's evidence, added to validation.md's Requirement → Verification Mapping. Task 1.1.1a just re-runs `escalation-reasoning.spec.ts --reporter=list` once; Task 1.1.1b cites "a run of that spec file's `--reporter=list` output" as proof AC7 ("matches its pre-migration pass count exactly") passes. A single run's count, with no stated baseline number, cannot by itself prove a match. (Independently verified for this review: `escalation-reasoning.spec.ts` was added to this branch fresh in commit `2811df54e` — `git show 2811df54e --name-status` shows status `A`, not `M` — because this branch predates upstream commit `303d43fad` (#315) where the file first landed on `main`. `git diff 303d43fad:tests/e2e/escalation-reasoning.spec.ts 2811df54e:tests/e2e/escalation-reasoning.spec.ts` shows the only delta is the intended dismissal-helper swap — so AC7 is in fact defensible — but the plan doesn't instruct this diff-based check or cite a specific expected count, so as written its evidence bar for AC7 is weaker than it needs to be.)
- [x] **Task 1.2.1b's "cycle 2 of at most 3" miscounts against `review.md`'s own convention.**
  - **Resolved**: plan.md's Task 1.2.1b now correctly labels this session's first `request_review` call as cycle 1, not cycle 2, and cites `review.md`'s per-session (not per-item) counter. `.claude/commands/backlog/review.md`: "Keep count of how many times you have run `/backlog/review` in THIS session — nothing tracks it for you." This plan runs in a session that has not yet called `request_review` (the prior FAIL came from a different, earlier implementation session). Task 1.2.1a's call is therefore this session's 1st `request_review`, not its 2nd — the plan's own branching text treats it as cycle 2, which would trigger the self-imposed "ship anyway" fallback one cycle earlier than `review.md` actually allows. Low-consequence (it errs toward shipping sooner, not looping longer), but it's a misapplication of the cited rule that should be corrected rather than left as a latent inconsistency.
- [x] **No fallback if `request_review` itself rejects or mishandles a call carrying a fail-marked criterion.**
  - **Resolved**: plan.md's Task 1.2.1a now has an explicit "Fallback if request_review errors or behaves unexpectedly" bullet — stop and surface to a human rather than silently flipping AC4 to pass. The tool's own description says "Call after all acceptance criteria are marked pass" — research/pitfalls.md §3 correctly identifies there's no better tool available and no rule prohibiting the deviation, and the plan follows that recommendation faithfully. But the description text's literal precondition is unverified from the outside — it's unknown whether it's advisory prose or backed by real server-side validation. Task 1.2.1a has no contingency for `request_review` erroring or behaving unexpectedly (e.g. silently coercing AC4 to a different state) when called this way for the first time in this codebase's SDD history (per research/architecture.md §3, this is "the first instance found... of two ACs in the same item being mutually exclusive").

## Minors

- Task 1.1.1b's example AC1 citation line numbers (27, 39, 65, 74, 84, 118, 132, 155) don't match current `HEAD` (`grep -n dismissOnboardingIfPresent tests/e2e/review-queue.spec.ts` shows 26, 36, 48, 72, 79, 116, 127, 152) — re-verify exact line numbers at `report_progress` time rather than reusing the plan's draft citations verbatim.
- AC5's evidence plan ("citing `OnboardingPage.ts`'s export and both spec files' imports") doesn't explicitly instruct citing the "plain function, not a class" structural detail that AC5's text specifically calls out, even though `OnboardingPage.ts` trivially satisfies it (`export async function dismissOnboardingIfPresent(page: Page): Promise<void>`, no class). Worth naming explicitly in the `report_progress` note since it's the one AC5 clause a lazy reviewer might skim past.
