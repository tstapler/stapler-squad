# Pre-mortem: flaky-hook-url-tests
**Date**: 2026-08-01

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | The already-documented, out-of-scope `testSocketOnce` shared-tmux-socket contention keeps causing these same two tests (or siblings sharing the socket) to fail intermittently after merge, and — because the PR's own success framing is "near-zero flakiness across 20 CI runs" with no distinguishing signal for *which* mechanism caused a given failure — every recurrence gets read as "the `-p 1`/60s fix didn't work" rather than "a different, already-known, explicitly out-of-scope mechanism is still live," provoking wasted re-investigation or a reflexive further timeout bump (90s, 120s...) that the file's own comment already warns is a trap. | A CI failure on `master`/a PR re-triggers this exact test pair weeks after merge, and the PR/issue discussion re-litigates "is `-p 1` insufficient?" instead of immediately recognizing the known residual cause. | Add one sentence to the shipping PR description and a code comment near `waitForTmuxTeardown` (which already documents `testSocketOnce`): "If `TestServer_should_Write*HookURL` flakes again after this PR, check whether it's the already-known shared-tmux-socket contention (see `testSocketOnce`/`waitForTmuxTeardown` comment) before re-tuning timeouts or CI topology — file a follow-up ticket for that root cause instead." | P1 |
| 2 | The 20-CI-run observation window passes cleanly by chance — no task in the plan actually captures a numeric before/after flake-rate baseline (Task 2.1.2 only measures wall-clock cost, not flakiness; Task 3.1.1 only documents a repro command, never runs it against unpatched code) — so the ticket gets marked "done" on a lucky streak, and real residual flakiness (contention still occasionally marginal, or the shared-socket issue from #1) resurfaces in month 2 with no baseline data to compare against, forcing the next investigator to redo the diagnostic work from scratch. | Failures reappear on `master` after the 20-run window was reported clean, with no historical flake-rate numbers in `validation.md` to tell whether this is "the fix regressed" or "the fix was never fully sufficient." | Add an explicit task (already flagged as missing by the adversarial review) that runs the documented stress-repro command — both plain `-count=10` and the artificial-load (`yes &`) variant — against the pre-fix code to record a baseline failure count, and again against the patched code, with both numbers written into `validation.md` before this ships. | P2 |
| 3 | `-p 1`'s wall-clock cost (measured once, locally, as "low single-digit minutes") compounds as more packages/tests are added to `./server/... ./session/... ./config/...` over time; a future engineer, annoyed by a slower `Test` job and without reading ADR-001 or this project's `project_plans/`, "helpfully" removes `-p 1` to speed up CI — silently reintroducing the cross-package `-race` contention this ticket was written to fix, with Race B flakiness returning some time later and being treated as a brand-new, unexplained regression. | A future PR's diff shows `-p 1` removed from `.github/workflows/build.yml` with a commit message like "speed up CI" or "reduce test job time," unaware of ADR-001; weeks/months later `TestServer_should_Write*HookURL` starts flaking again with no obvious code change nearby. | Add an inline comment directly next to `-p 1` in `.github/workflows/build.yml` (not just in the ADR): `# -p 1: serializes package binaries to reduce -race CPU contention that caused intermittent hook/MCP-URL test timeouts — see project_plans/flaky-hook-url-tests/decisions/ADR-001. Do not remove without re-reading that ADR and re-verifying flake rate.` | P2 |
| 4 | A future contributor, chasing perceived tmux spin-up slowness for an unrelated reason, tunes `sessionCreateTimeout` in `session/tmux/tmux.go` (Race A's territory, explicitly out of scope here) without realizing it's calibrated against both the fake `claude` binary's `sleep 60` workaround and this ticket's new 60s `waitForPermissionRequestHookCommand` budget — shrinking it silently reintroduces Race A's early-exit hang, which manifests in CI logs as the *same* test timing out that this ticket just fixed for a different reason (Race B), causing confusion about which race regressed. | `TestServer_should_Write*HookURL` starts failing again, but server logs now show `"[CreateSession] async start failed"` (Race A's signature) instead of a clean timeout with no such log line (Race B's signature) — a distinction this ticket's own glossary defines but that a future investigator may not think to check first. | Add a cross-reference comment at `sessionCreateTimeout`'s definition (`session/tmux/tmux.go:182-183`) pointing at `installFakeClaudeBinary`'s `sleep 60` workaround and this ticket's 60s budget, so a future change to that constant is forced to notice the coupling before touching it. | P3 |
| 5 | The `require.Eventually` conversion changes the exact failure-message text/format for these tests' timeout errors (testify's "Condition never satisfied" wrapper instead of the hand-rolled `t.Fatalf` string) — if anything in the repo greps CI logs or test output for the old literal message (a flaky-test triage script, a saved CI-log search, an alert), it silently stops recognizing recurrences of this exact failure as "known," so the next occurrence gets mis-triaged as a brand-new, unknown failure. | A future flaky-test dashboard/triage tool fails to bucket a recurrence of this failure with its history, or a saved log search a maintainer relies on stops matching. | Before merging, grep the repo (CI configs, any triage/dashboard scripts, saved searches referenced in docs) for the old `t.Fatalf` message text from these four helpers to confirm nothing string-matches on it; if something does, update it in the same PR. Add this as an explicit check-off item in `validation.md`. | P3 |

## P1 Items (address before implementation)
- [ ] #1 — Add a PR-description sentence and a code comment near `waitForTmuxTeardown` explicitly pre-registering the known, unfixed `testSocketOnce` shared-socket mechanism as the presumptive cause of any post-merge recurrence, with a pointer to file a follow-up ticket rather than re-tuning timeouts or CI topology in response.
