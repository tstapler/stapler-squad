# Pre-mortem: launchd-shell-sourcing

**Date**: 2026-07-25

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | BUG-006 entry is worded so it reads as "the gap is fixed" (reusing `**Mitigation:**` verbatim like BUG-001..005), silently declaring AC5's own "not silently declared resolved" requirement violated | A future contributor sees BUG-006 in `completed/` docs and assumes `ANTHROPIC_API_KEY` inheritance already works, ships a feature depending on it, then it fails at runtime with no env var | Use `**Status:** Deferred — not implemented this session` + relabeled `**Possible Future Mitigations (not implemented):**` heading (already in plan.md Pattern Decisions, addressed by architecture-review remediation) | P2 |
| 2 | Criteria 3/4 (archive + cross-link) get reported as `pass` via a workaround (e.g. hand-editing backlog state through some other channel) instead of honestly blocked, corrupting shared backlog data or conflicting with the in-flight `expose-archivebacklogitem` branch | Canonical item 1dc7ff10's notes get a malformed/duplicate cross-link, or this item shows `archived` without ever having gone through the real RPC | Explicitly call `/backlog/fail-3` and `/backlog/fail-4` with reasoning naming the missing tool; never call backend RPCs out-of-band (requirements.md R3) | P1 |
| 3 | Doc entry recommends a `/bin/sh` wrapper as a "safe" mitigation option, and a future maintainer implements it verbatim, reintroducing a shell-wrapper `ProgramArguments`/`ExecStart` — the same class of regression this whole backlog item exists to prevent | A future PR adds `. ~/.stapler-squad/env && exec $bin_path` to the plist/unit; launchd/systemd jobs start hanging or behaving non-deterministically again, same symptom as the original bug | Architecture-review remediation applied to plan.md Task 2.1: drop the wrapper bullet, keep only `EnvironmentFile=` (systemd) + install-time plist inlining (macOS), per `research/build-vs-buy.md` §4 | P2 |
| 4 | This session claims all 6 criteria "done" in `request_review` without distinguishing blocked-vs-passed, and the reviewer can't tell which is which from the diff alone | Reviewer marks criteria UNVERIFIABLE (already happened once, per the item's prior FAIL verdict) and the item bounces for another cycle | `request_review` message and `verification_notes` explicitly enumerate per-criterion disposition (pass/blocked) with citations, matching validation.md's coverage table | P1 |
| 5 | The duplicate/canonical relationship (this item vs. `1dc7ff10`) is itself wrong — i.e. this item has scope not actually covered by the canonical item — and closing it as a duplicate loses real, untracked work | Someone later looks for a "why was the zsh sourcing fix tracked" item and can't find it because it was archived as a pure duplicate with no distinguishing content preserved | Not this session's call to make or unmake — already established by a prior review pass as a duplicate; this session only executes the closeout, doesn't re-litigate the duplicate determination. If in doubt, the blocked-criteria reporting (failure #2's prevention) means no data is silently lost even if the determination is later found wrong | P3 |

## P1 Items (address before implementation)

- [x] **Failure #2** — plan.md Story 3 already scopes criteria 3/4 as explicit `/backlog/fail-N` reports with named reasoning, not a workaround. No plan change needed; confirm this is followed at implementation time.
- [x] **Failure #4** — `request_review` call (Story-level, not yet in plan.md as a discrete task) must enumerate per-criterion disposition. Adding this as an explicit implementation-time requirement: the `request_review` message must list all 6 criteria with pass/blocked and citations, mirroring validation.md's coverage table, not a generic "done" summary.
