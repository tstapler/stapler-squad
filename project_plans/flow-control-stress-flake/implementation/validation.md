
# Validation Plan: flow-control-stress-flake

**Date**: 2026-08-29

## Happy Path Scenario

Given the current flaky state (5000 real-timer round-trips serialized in the test's critical path), when the plan's 9 tasks are applied to `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts`, then `npx jest --testPathPatterns='flow-control-stress' --no-coverage` passes deterministically regardless of concurrent CPU load, with no other test in the file or suite affected.

## Requirement → Test Mapping

| Requirement | Test File | Test Name | Type | Scenario |
|-------------|-----------|-----------|------|----------|
| AC1: no real-timer dependency in the critical path | `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` | `flow-control-stress.test.ts_should_have_no_unadvanced_real_timer_await_When_grepped_for_setTimeout_in_touched_describes` | Code-check (grep + read) | Run `grep -n "setTimeout" web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` post-patch and inspect each hit. Every remaining `setTimeout` call must be either (a) inside `WatermarkTracker.write()` itself (line 32 — production-equivalent test scaffolding, not a test-body await, and now driven by fake timers from the describe-level `beforeEach`), or (b) absent entirely from the `Mixed Content Stress` and `Control Code Heavy Output` describe bodies. Confirm no `await new Promise<void>((resolve) => { tracker.write(...) })` pattern remains unpaired with `jest.advanceTimersByTimeAsync(...)` in those two blocks. As a second, executable confirmation of the same property: run `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage --testTimeout=2000` — if the test still depended on any real, un-advanced timer, dropping the timeout to 2000ms (below the ~5000ms real-timer floor documented in requirements.md) would make it fail/timeout; passing at 2000ms is positive evidence the critical path no longer depends on real wall-clock timer scheduling. |
| AC2: same iteration count/shape/code path preserved | `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` | `flow-control-stress.test.ts_should_preserve_iteration_count_and_processChunk_call_shape_When_diffed_against_pre_patch_version` | Diff-level check | Run `git diff -- web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` after the patch and confirm the diff touches only: the two new `beforeEach`/`afterEach` timer-scoping blocks (Tasks 1.1.1a, 1.1.2a), the three write-await replacements (Tasks 1.1.1b, 1.1.2b, 1.1.2c), the periodic-yield replacement (Task 1.1.1c), and the animation frame-delay replacement (part of Task 1.1.2b). Confirm `iterations = 5000` (Mixed Content Stress), the chunk-construction `if/else if/else` block, and every `parser.processChunk(chunk)` call site are absent from the diff (i.e. byte-identical before/after) — only the timer-wait mechanism changed. |
| AC3: assertions still catch a real regression | `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` | `handles_alternating_text_and_control_codes_should_fail_When_watermark_decrement_is_deleted` | Mutation check (manual, temporary) | Using the corrected regression from `adversarial-review.md` (not the original `LOW_WATERMARK`-check example, which doesn't perturb the asserted value): temporarily delete the decrement `this.watermark = Math.max(0, this.watermark - data.length);` at line 33 inside `WatermarkTracker.write()`, leaving the rest of the class untouched. Run `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage -t "handles alternating text and control codes"` and confirm it **fails** (watermark grows monotonically past 50000, tripping `expect(metrics.watermark).toBeLessThan(50000)` at line 290). Then `git checkout -- web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` (or revert the one-line edit) and re-run the same command, confirming it **passes**. This proves the fake-timer swap changed only the await mechanism, not what's asserted. |
| AC4: fix generalizes to the two Control Code Heavy Output tests | `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` | `Control_Code_Heavy_Output_tests_should_pass_individually_When_run_with_scoped_fake_timers` | CLI-check | Run each of the two generalized tests in isolation: `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage -t "handles Claude Code style animations"` and `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage -t "handles cursor positioning sequences"`. Confirm both pass, and confirm (by reading the patched file) both now use the same `jest.useFakeTimers()`-scoped `Promise.all([writeDone, jest.advanceTimersByTimeAsync(...)])` race pattern as the primary test (Tasks 1.1.2a/b/c), not a bespoke mechanism. |
| AC5: passes reliably including under contention | `web-app/src/lib/terminal/__tests__/flow-control-stress.test.ts` | `flow-control-stress_suite_should_pass_repeatedly_and_under_synthetic_CPU_contention` | CLI-check (stress repro) | Two-part check, using the corrected Task 1.1.3 repro (not `make proto-gen`, which is stamp-guarded and a near-instant no-op by this point per `adversarial-review.md`): (1) `cd web-app && for i in 1 2 3 4 5; do npx jest --testPathPatterns="flow-control-stress" --no-coverage || break; done` — confirm all 5 runs report 11/11 passing. (2) Contention repro: `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage & JEST_PID=$!; for i in $(seq 1 "$(nproc)"); do yes > /dev/null & done; wait $JEST_PID; EXIT=$?; kill $(jobs -p) 2>/dev/null; exit $EXIT` (or `stress-ng --cpu "$(nproc)" --timeout 30s &` as an alternative CPU-load generator). Confirm the foregrounded Jest run exits 0 despite concurrent CPU saturation. |
| AC6: no suite regression | `web-app` full suite | `web-app_jest_suite_should_have_no_new_failures_When_run_full_after_patch` | CLI-check (full-suite diff) | Capture a pre-patch baseline pass/fail list (`cd web-app && npx jest --no-coverage --json --outputFile=/tmp/jest-baseline.json` on the unpatched tree, or reconstruct from the last known-green CI run), then run `cd web-app && npx jest --no-coverage --json --outputFile=/tmp/jest-postpatch.json` on the patched tree. Diff the two `testResults[].assertionResults[].status` sets — confirm zero previously-passing tests transitioned to failing, and specifically confirm the file's other 8 untouched tests (`Large Volume Tests`, `Rapid Small Writes`, `Watermark Behavior`, `Error Recovery` describes) remain passing, proving the `beforeEach`/`afterEach` fake-timer scoping in the two touched describes did not leak. |

## UX Acceptance Tests
N/A — no user-facing surface (test-infrastructure-only fix).

## Test Stack
- **Unit**: Jest 30.2.0 (ts-jest) — this project's own test-reliability checks run via the same Jest CLI (`npx jest --testPathPatterns`), not a separate unit-test harness, since the "unit under test" here is the test file's own timer behavior.
- **Integration**: N/A — no production code path changed (`WatermarkTracker` is in-file test scaffolding; `EscapeSequenceParser` is untouched).
- **E2E / UX**: N/A — no user-facing surface.

## Coverage Targets and How to Measure

Not a line-coverage-driven change (no new production code, no coverage threshold applies). "Done" is defined as:
- All 6 checks above pass (AC1–AC6, one check per requirement, 6/6).
- `cd web-app && npx jest --testPathPatterns="flow-control-stress" --no-coverage` passes on 5 consecutive isolated runs (AC5, part 1) and on at least one run under synthetic CPU contention (AC5, part 2).
- `cd web-app && npx jest --no-coverage` (full suite) shows zero regressions relative to the pre-patch baseline (AC6).
- The AC3 mutation check is reverted (`git status` shows no leftover edit to `WatermarkTracker.write()`) before the change is considered complete.
