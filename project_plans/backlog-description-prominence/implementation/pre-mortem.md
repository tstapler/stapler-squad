# Pre-mortem: backlog-description-prominence
**Date**: 2026-08-01

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | The plan's file list and Verification section omit `tests/e2e/backlog-item-detail-redesign.spec.ts`, whose test `"expands a top-level section from its own default-collapsed state"` (lines 96–111) hardcodes `expect(detailPage.sectionHeader("description")).toHaveAttribute("aria-expanded", "false")` as a precondition (with a comment literally citing the old "Story 3.1.3" collapsed-by-default behavior) before clicking to expand it. Confirmed by grep — this file was never opened during planning. | The e2e CI workflow (`.github/workflows/e2e-video.yml`, separate from `make ci`) goes red on this PR; if the PR is merged without waiting for that check (or if it's misread as unrelated flake), the spec stays broken on `main` until someone investigates. | Add this file to the plan's "Files" list and Verification section; update the test to assert `aria-expanded="true"` on load (no stored preference) and rename/rewrite it to cover the *collapse* interaction instead of the *expand* interaction — e.g. click to collapse, assert `"false"`, click again to re-expand. Grep for `aria-expanded.*description\|description.*aria-expanded\|Story 3.1.3` across `tests/e2e/` as a plan-time step, not just `web-app/src`. | P1 |
| 2 | A future contributor (or an LLM finishing this same task) applies Task 1.1.1a's edit by line number only, without re-reading the surrounding function, and the `descriptionExpanded` variable (used again at line 374 for `sectionExpandEntries`) drifts out of sync if a different edit is made to the same block first — e.g. an unrelated PR reorders hook calls or the file grows past line 323 between plan-writing and implementation. | Build succeeds but `sectionExpandEntries`/`openSectionKeys` silently still resolves to the old default because the edit landed on the wrong `useSectionExpandState(...)` call (there are multiple sections following the identical pattern), so Description stays collapsed and the fix appears to do nothing in manual QA. | Task 1.1.1a already anchors on the full unique source string (not just the line number) — confirmed current file content matches the plan's quoted old_string verbatim, so an Edit-tool-based application is safe today. Prevention is process, not code: implementer must re-`grep -n 'useSectionExpandState(itemId, "description"'` immediately before editing rather than trusting the cited line number, since the plan itself will go stale the moment any earlier line in the file changes. | P3 |
| 3 | Task 1.3.1a's three test rewrites in `DescriptionSection.test.tsx` are easy to cross the assertions on (e.g. the empty-state test keeps `defaultExpanded={true}` per the plan but an implementer accidentally leaves the old `queryByTestId(...).not.toBeInTheDocument()` assertion in the *first* test instead of removing it, since three near-identical test bodies are being edited in the same pass). | `npx jest DescriptionSection` fails locally right after the edit — caught before commit, not a shipped regression — but if the implementer runs the wrong `--testPathPatterns` filter (e.g. only `BacklogItemDetail`, matching the plan's Verification command literally, which does include `DescriptionSection` — so this is actually caught) the mismatch could slip into a commit that's later force-fixed in a follow-up. | Keep Verification's combined `--testPathPatterns="DescriptionSection|BacklogItemDetail.markdown|BacklogItemDetail.test"` command as the mandatory final check before commit (it already covers this file) rather than running each file's tests in isolation during the edit pass. | P3 |
| 4 | The `CollapsibleSection` dev-mode divergence check (`Collapsible.tsx:139-157`) fires `console.warn` if Task 1.2.1b's `defaultExpanded={descriptionExpanded}` ever disagrees with the group's own `value` (e.g. if Epic 1.1's flip and Epic 1.2's prop-threading land out of order, or a future edit changes one without the other). | Console noise in dev/test runs (`jest.setup.js` confirmed to have no console.warn-throws-test override, so this would NOT fail CI) — the more realistic symptom is a developer seeing an unexplained warning in the browser console during manual QA and not knowing which of the two flip points is stale. | The plan's own Dependency Visualization already sequences 1.1.1a before 1.2.1b, which is the correct prevention; no plan change needed beyond keeping that ordering intact during implementation (don't parallelize 1.1 and 1.2). | P3 |
| 5 | `DescriptionSection`'s stale docstring update (Task 1.2.1a) is a prose/comment-only change with no test asserting its content, so if the implementer applies the code changes but skips the exact doc-comment replacement (easy to do since it's the least "functional" part of the task), the component ships with the new `true`-by-default behavior but the old "collapsed by default (Story 3.1.3, Task 3.1.3a)" comment still in place — reintroducing the exact misleading-dead-code problem Approach A was rejected for. | No automated symptom — only caught by a human (code review) noticing the docstring still says "collapsed by default" next to a prop that's now threaded as `true`. | Add the docstring text as an explicit line in the PR's self-review checklist / `sdd:6-verify` pass, since there's no lint rule that can catch stale prose; alternatively, fold the docstring assertion into a lightweight test (e.g. a `.test.tsx` snapshot of the exported doc comment is overkill, but a PR template checkbox "docstring reflects new default" is proportionate). | P3 |

## P1 Items (address before implementation)
- [x] #1 — Resolved during Phase 4 (sdd:4-validate): added Epic 1.5 (Story 1.5.1, Task 1.5.1a) to plan.md, rewriting `tests/e2e/backlog-item-detail-redesign.spec.ts`'s stale test, and added it to plan.md's Verification section. See `implementation/plan.md`'s Epic 1.5.
