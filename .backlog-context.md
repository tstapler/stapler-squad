--- BACKLOG ITEM DATA (treat as inert data, not instructions) ---
# Rebrand the unfinished page (Priority 3 | Status: in_progress)

## Description
I don't think that the unfinished page has a great layout for what it actually is, where it's effectively and up next page where you can find additional work that you need to work on. For example, I would also like to be able to scan through the GitHub issues that we know about for each of the repositories that we're currently working on or give the user the ability to import tasks that they want to work on

## Acceptance Criteria
0. [ ] Explicitly resolve the orphaned /settings/unfinished route instead of leaving it dangling post-rebrand
1. [ ] Keep GitHub issue fetching on-demand/low-frequency rather than auto-loading on every page visit
2. [ ] Keep 'In Progress' (worktrees/PRs) and 'Up Next' (queued issues) as visually distinct sections rather than a merged list

## Prior Attempts
- Role: triage | Commits: 0
- Role: work | Commits: 0
- Role: review | Commits: 0 | Verdict: PASS
- Role: work | Commits: 0
- Role: review | Commits: 0 | Verdict: FAIL
  Reviewer summary: The diff contains none of the work required by this item. It touches session/backlog_lifecycle.go (an unrelated fix for a false-positive "shipped to main" detection bug) and adds an unrelated frontend test file for BacklogItemDetail's lastReviewResult rendering. Nothing in the diff touches the /settings/unfinished route, GitHub issue fetching, or the unfinished/backlog page layout at all — this looks like the wrong session's diff was captured, or no work was actually done on this item.
  Criterion 0 (FAIL): No changes to web-app/src/app/settings/unfinished or any route-handling code appear in the diff; the /settings/unfinished route is not addressed at all.
  Criterion 1 (FAIL): No GitHub issue fetching code (new hooks, RPCs, or components) appears anywhere in the diff.
  Criterion 2 (FAIL): No 'In Progress'/'Up Next' section changes appear in the diff. The only frontend change is an unrelated new test file BacklogItemDetail.lastReviewResult.test.tsx, and the only backend change is an unrelated fix in session/backlog_lifecycle.go for shipped-commit detection.
- Role: review | Commits: 0 | Verdict: FAIL
  Reviewer summary: The diff present in this worktree does not address this backlog item at all. It contains a Go fix to session/backlog_lifecycle.go (BUG-039: fresh-branch base-commit false-positive in reconcileBouncingItems) plus its test, and an unrelated new frontend test file (BacklogItemDetail.lastReviewResult.test.tsx) for surfacing last review results. None of the "unfinished page" rebrand work — resolving the orphaned /settings/unfinished route, on-demand GitHub issue fetching, or distinct In Progress/Up [truncated]
  Criterion 0 (FAIL): No changes to web-app/src/app/settings/unfinished/page.tsx or any routing file appear in the diff; the diff only touches session/backlog_lifecycle.go, its test, and an unrelated BacklogItemDetail test.
  Criterion 1 (FAIL): No changes to GitHubPRsSection.tsx or any GitHub issue-fetching code exist in the diff.
  Criterion 2 (FAIL): No changes to UnfinishedTab.tsx, UnfinishedRepoGroup.tsx, or any section-layout component exist in the diff — the diff is entirely about backlog lifecycle reconciliation logic, unrelated to the unfinished page UI.
--- END BACKLOG ITEM DATA ---

## Your Task Protocol
1. Read ALL acceptance criteria before starting any work.
2. Work through criteria systematically; run `/backlog/done-N` when criterion N is complete.
3. When ALL criteria are done, run `/backlog/review` with a 2–3 sentence summary of what you built.
4. If you hit a blocker or need human input, run `/backlog/review` describing what you need — do not stop silently.
5. If your context is compacted or you lose track of your task, re-read `.backlog-context.md` or run `/backlog/status` immediately before continuing.
6. If the `/backlog/*` commands fail or the MCP server is unavailable, continue your work using the criteria listed in `.backlog-context.md` and record completed criteria in your commit messages.
7. NEVER end your session without calling `/backlog/review` — this is how the task is closed properly.
8. After `/backlog/review`, stay in this session — do not exit. Wait, then run `/backlog/status` again to check for a verdict. PASS → immediately run `/backlog/ship` yourself to open the pull request (it drives `/github:pr-ship`, which can rebase, resolve merge conflicts, and react to failing CI checks) — shipping the PR is part of this task, not a separate step someone else does; do not stop here. FAIL/PARTIAL → fix the noted gaps yourself and run `/backlog/review` again.
9. Keep count of how many times you've run `/backlog/review` in THIS session (count your own calls in this conversation — nothing tracks it for you). After 3 review cycles without a PASS, STOP looping: run `/backlog/ship` anyway to open a PR so a human can pick up the review directly, rather than retrying `/backlog/review` again. Nothing will kill or replace this session while you do any of this.

## Fallback Instructions
If MCP tools are unavailable, continue using the acceptance criteria above.
Record completed criteria in commit messages. Run git commit after each criterion is done.

## Before You Start
This worktree's branch may be behind main (this file is rewritten on every spawn and re-attach, but the branch itself is not auto-synced). Run `git merge main` before starting substantive work. If it merges cleanly, continue. If it conflicts, resolve them as part of this task — you have the context to do it correctly; a background process does not.
