// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/tstapler/stapler-squad/session/ent/backlogitem"
	"github.com/tstapler/stapler-squad/session/ent/itemsource"
	"github.com/tstapler/stapler-squad/session/ent/predicate"
	"github.com/tstapler/stapler-squad/session/ent/sourcesyncevent"
)

// ItemSourceQuery is the builder for querying ItemSource entities.
type ItemSourceQuery struct {
	config
	ctx              *QueryContext
	order            []itemsource.OrderOption
	inters           []Interceptor
	predicates       []predicate.ItemSource
	withBacklogItems *BacklogItemQuery
	withSyncEvents   *SourceSyncEventQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ItemSourceQuery builder.
func (_q *ItemSourceQuery) Where(ps ...predicate.ItemSource) *ItemSourceQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ItemSourceQuery) Limit(limit int) *ItemSourceQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ItemSourceQuery) Offset(offset int) *ItemSourceQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ItemSourceQuery) Unique(unique bool) *ItemSourceQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ItemSourceQuery) Order(o ...itemsource.OrderOption) *ItemSourceQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBacklogItems chains the current query on the "backlog_items" edge.
func (_q *ItemSourceQuery) QueryBacklogItems() *BacklogItemQuery {
	query := (&BacklogItemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(itemsource.Table, itemsource.FieldID, selector),
			sqlgraph.To(backlogitem.Table, backlogitem.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, itemsource.BacklogItemsTable, itemsource.BacklogItemsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySyncEvents chains the current query on the "sync_events" edge.
func (_q *ItemSourceQuery) QuerySyncEvents() *SourceSyncEventQuery {
	query := (&SourceSyncEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(itemsource.Table, itemsource.FieldID, selector),
			sqlgraph.To(sourcesyncevent.Table, sourcesyncevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, itemsource.SyncEventsTable, itemsource.SyncEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ItemSource entity from the query.
// Returns a *NotFoundError when no ItemSource was found.
func (_q *ItemSourceQuery) First(ctx context.Context) (*ItemSource, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{itemsource.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ItemSourceQuery) FirstX(ctx context.Context) *ItemSource {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ItemSource ID from the query.
// Returns a *NotFoundError when no ItemSource ID was found.
func (_q *ItemSourceQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{itemsource.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ItemSourceQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ItemSource entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ItemSource entity is found.
// Returns a *NotFoundError when no ItemSource entities are found.
func (_q *ItemSourceQuery) Only(ctx context.Context) (*ItemSource, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{itemsource.Label}
	default:
		return nil, &NotSingularError{itemsource.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ItemSourceQuery) OnlyX(ctx context.Context) *ItemSource {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ItemSource ID in the query.
// Returns a *NotSingularError when more than one ItemSource ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ItemSourceQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{itemsource.Label}
	default:
		err = &NotSingularError{itemsource.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ItemSourceQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ItemSources.
func (_q *ItemSourceQuery) All(ctx context.Context) ([]*ItemSource, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ItemSource, *ItemSourceQuery]()
	return withInterceptors[[]*ItemSource](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ItemSourceQuery) AllX(ctx context.Context) []*ItemSource {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ItemSource IDs.
func (_q *ItemSourceQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(itemsource.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ItemSourceQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ItemSourceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ItemSourceQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ItemSourceQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ItemSourceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ItemSourceQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ItemSourceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ItemSourceQuery) Clone() *ItemSourceQuery {
	if _q == nil {
		return nil
	}
	return &ItemSourceQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]itemsource.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.ItemSource{}, _q.predicates...),
		withBacklogItems: _q.withBacklogItems.Clone(),
		withSyncEvents:   _q.withSyncEvents.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithBacklogItems tells the query-builder to eager-load the nodes that are connected to
// the "backlog_items" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ItemSourceQuery) WithBacklogItems(opts ...func(*BacklogItemQuery)) *ItemSourceQuery {
	query := (&BacklogItemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBacklogItems = query
	return _q
}

// WithSyncEvents tells the query-builder to eager-load the nodes that are connected to
// the "sync_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ItemSourceQuery) WithSyncEvents(opts ...func(*SourceSyncEventQuery)) *ItemSourceQuery {
	query := (&SourceSyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSyncEvents = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PluginID string `json:"plugin_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ItemSource.Query().
//		GroupBy(itemsource.FieldPluginID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ItemSourceQuery) GroupBy(field string, fields ...string) *ItemSourceGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ItemSourceGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = itemsource.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PluginID string `json:"plugin_id,omitempty"`
//	}
//
//	client.ItemSource.Query().
//		Select(itemsource.FieldPluginID).
//		Scan(ctx, &v)
func (_q *ItemSourceQuery) Select(fields ...string) *ItemSourceSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ItemSourceSelect{ItemSourceQuery: _q}
	sbuild.label = itemsource.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ItemSourceSelect configured with the given aggregations.
func (_q *ItemSourceQuery) Aggregate(fns ...AggregateFunc) *ItemSourceSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ItemSourceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !itemsource.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ItemSourceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ItemSource, error) {
	var (
		nodes       = []*ItemSource{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withBacklogItems != nil,
			_q.withSyncEvents != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ItemSource).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ItemSource{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBacklogItems; query != nil {
		if err := _q.loadBacklogItems(ctx, query, nodes,
			func(n *ItemSource) { n.Edges.BacklogItems = []*BacklogItem{} },
			func(n *ItemSource, e *BacklogItem) { n.Edges.BacklogItems = append(n.Edges.BacklogItems, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSyncEvents; query != nil {
		if err := _q.loadSyncEvents(ctx, query, nodes,
			func(n *ItemSource) { n.Edges.SyncEvents = []*SourceSyncEvent{} },
			func(n *ItemSource, e *SourceSyncEvent) { n.Edges.SyncEvents = append(n.Edges.SyncEvents, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ItemSourceQuery) loadBacklogItems(ctx context.Context, query *BacklogItemQuery, nodes []*ItemSource, init func(*ItemSource), assign func(*ItemSource, *BacklogItem)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ItemSource)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.BacklogItem(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(itemsource.BacklogItemsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.item_source_backlog_items
		if fk == nil {
			return fmt.Errorf(`foreign-key "item_source_backlog_items" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "item_source_backlog_items" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ItemSourceQuery) loadSyncEvents(ctx context.Context, query *SourceSyncEventQuery, nodes []*ItemSource, init func(*ItemSource), assign func(*ItemSource, *SourceSyncEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ItemSource)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.SourceSyncEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(itemsource.SyncEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.item_source_sync_events
		if fk == nil {
			return fmt.Errorf(`foreign-key "item_source_sync_events" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "item_source_sync_events" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ItemSourceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ItemSourceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(itemsource.Table, itemsource.Columns, sqlgraph.NewFieldSpec(itemsource.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, itemsource.FieldID)
		for i := range fields {
			if fields[i] != itemsource.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ItemSourceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(itemsource.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = itemsource.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ItemSourceGroupBy is the group-by builder for ItemSource entities.
type ItemSourceGroupBy struct {
	selector
	build *ItemSourceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ItemSourceGroupBy) Aggregate(fns ...AggregateFunc) *ItemSourceGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ItemSourceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemSourceQuery, *ItemSourceGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ItemSourceGroupBy) sqlScan(ctx context.Context, root *ItemSourceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ItemSourceSelect is the builder for selecting fields of ItemSource entities.
type ItemSourceSelect struct {
	*ItemSourceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ItemSourceSelect) Aggregate(fns ...AggregateFunc) *ItemSourceSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ItemSourceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemSourceQuery, *ItemSourceSelect](ctx, _s.ItemSourceQuery, _s, _s.inters, v)
}

func (_s *ItemSourceSelect) sqlScan(ctx context.Context, root *ItemSourceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
