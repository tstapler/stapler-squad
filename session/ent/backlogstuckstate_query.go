// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/tstapler/stapler-squad/session/ent/backlogitem"
	"github.com/tstapler/stapler-squad/session/ent/backlogstuckstate"
	"github.com/tstapler/stapler-squad/session/ent/predicate"
)

// BacklogStuckStateQuery is the builder for querying BacklogStuckState entities.
type BacklogStuckStateQuery struct {
	config
	ctx        *QueryContext
	order      []backlogstuckstate.OrderOption
	inters     []Interceptor
	predicates []predicate.BacklogStuckState
	withItem   *BacklogItemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BacklogStuckStateQuery builder.
func (_q *BacklogStuckStateQuery) Where(ps ...predicate.BacklogStuckState) *BacklogStuckStateQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BacklogStuckStateQuery) Limit(limit int) *BacklogStuckStateQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BacklogStuckStateQuery) Offset(offset int) *BacklogStuckStateQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BacklogStuckStateQuery) Unique(unique bool) *BacklogStuckStateQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BacklogStuckStateQuery) Order(o ...backlogstuckstate.OrderOption) *BacklogStuckStateQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryItem chains the current query on the "item" edge.
func (_q *BacklogStuckStateQuery) QueryItem() *BacklogItemQuery {
	query := (&BacklogItemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(backlogstuckstate.Table, backlogstuckstate.FieldID, selector),
			sqlgraph.To(backlogitem.Table, backlogitem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, backlogstuckstate.ItemTable, backlogstuckstate.ItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BacklogStuckState entity from the query.
// Returns a *NotFoundError when no BacklogStuckState was found.
func (_q *BacklogStuckStateQuery) First(ctx context.Context) (*BacklogStuckState, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{backlogstuckstate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) FirstX(ctx context.Context) *BacklogStuckState {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BacklogStuckState ID from the query.
// Returns a *NotFoundError when no BacklogStuckState ID was found.
func (_q *BacklogStuckStateQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{backlogstuckstate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BacklogStuckState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BacklogStuckState entity is found.
// Returns a *NotFoundError when no BacklogStuckState entities are found.
func (_q *BacklogStuckStateQuery) Only(ctx context.Context) (*BacklogStuckState, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{backlogstuckstate.Label}
	default:
		return nil, &NotSingularError{backlogstuckstate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) OnlyX(ctx context.Context) *BacklogStuckState {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BacklogStuckState ID in the query.
// Returns a *NotSingularError when more than one BacklogStuckState ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BacklogStuckStateQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{backlogstuckstate.Label}
	default:
		err = &NotSingularError{backlogstuckstate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BacklogStuckStates.
func (_q *BacklogStuckStateQuery) All(ctx context.Context) ([]*BacklogStuckState, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BacklogStuckState, *BacklogStuckStateQuery]()
	return withInterceptors[[]*BacklogStuckState](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) AllX(ctx context.Context) []*BacklogStuckState {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BacklogStuckState IDs.
func (_q *BacklogStuckStateQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(backlogstuckstate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BacklogStuckStateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BacklogStuckStateQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BacklogStuckStateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BacklogStuckStateQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BacklogStuckStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BacklogStuckStateQuery) Clone() *BacklogStuckStateQuery {
	if _q == nil {
		return nil
	}
	return &BacklogStuckStateQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]backlogstuckstate.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.BacklogStuckState{}, _q.predicates...),
		withItem:   _q.withItem.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithItem tells the query-builder to eager-load the nodes that are connected to
// the "item" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BacklogStuckStateQuery) WithItem(opts ...func(*BacklogItemQuery)) *BacklogStuckStateQuery {
	query := (&BacklogItemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withItem = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ItemID uuid.UUID `json:"item_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BacklogStuckState.Query().
//		GroupBy(backlogstuckstate.FieldItemID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BacklogStuckStateQuery) GroupBy(field string, fields ...string) *BacklogStuckStateGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BacklogStuckStateGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = backlogstuckstate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ItemID uuid.UUID `json:"item_id,omitempty"`
//	}
//
//	client.BacklogStuckState.Query().
//		Select(backlogstuckstate.FieldItemID).
//		Scan(ctx, &v)
func (_q *BacklogStuckStateQuery) Select(fields ...string) *BacklogStuckStateSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BacklogStuckStateSelect{BacklogStuckStateQuery: _q}
	sbuild.label = backlogstuckstate.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BacklogStuckStateSelect configured with the given aggregations.
func (_q *BacklogStuckStateQuery) Aggregate(fns ...AggregateFunc) *BacklogStuckStateSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BacklogStuckStateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !backlogstuckstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BacklogStuckStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BacklogStuckState, error) {
	var (
		nodes       = []*BacklogStuckState{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withItem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BacklogStuckState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BacklogStuckState{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withItem; query != nil {
		if err := _q.loadItem(ctx, query, nodes, nil,
			func(n *BacklogStuckState, e *BacklogItem) { n.Edges.Item = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BacklogStuckStateQuery) loadItem(ctx context.Context, query *BacklogItemQuery, nodes []*BacklogStuckState, init func(*BacklogStuckState), assign func(*BacklogStuckState, *BacklogItem)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BacklogStuckState)
	for i := range nodes {
		fk := nodes[i].ItemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(backlogitem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "item_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *BacklogStuckStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BacklogStuckStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(backlogstuckstate.Table, backlogstuckstate.Columns, sqlgraph.NewFieldSpec(backlogstuckstate.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, backlogstuckstate.FieldID)
		for i := range fields {
			if fields[i] != backlogstuckstate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withItem != nil {
			_spec.Node.AddColumnOnce(backlogstuckstate.FieldItemID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BacklogStuckStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(backlogstuckstate.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = backlogstuckstate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BacklogStuckStateGroupBy is the group-by builder for BacklogStuckState entities.
type BacklogStuckStateGroupBy struct {
	selector
	build *BacklogStuckStateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BacklogStuckStateGroupBy) Aggregate(fns ...AggregateFunc) *BacklogStuckStateGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BacklogStuckStateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BacklogStuckStateQuery, *BacklogStuckStateGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BacklogStuckStateGroupBy) sqlScan(ctx context.Context, root *BacklogStuckStateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BacklogStuckStateSelect is the builder for selecting fields of BacklogStuckState entities.
type BacklogStuckStateSelect struct {
	*BacklogStuckStateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BacklogStuckStateSelect) Aggregate(fns ...AggregateFunc) *BacklogStuckStateSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BacklogStuckStateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BacklogStuckStateQuery, *BacklogStuckStateSelect](ctx, _s.BacklogStuckStateQuery, _s, _s.inters, v)
}

func (_s *BacklogStuckStateSelect) sqlScan(ctx context.Context, root *BacklogStuckStateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
