// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/tstapler/stapler-squad/session/ent/sessiongoal"
)

// SessionGoal is the model entity for the SessionGoal schema.
type SessionGoal struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SessionUUID holds the value of the "session_uuid" field.
	SessionUUID string `json:"session_uuid,omitempty"`
	// Goal holds the value of the "goal" field.
	Goal string `json:"goal,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// JSON []TaskNode
	Tasks string `json:"tasks,omitempty"`
	// SetBy holds the value of the "set_by" field.
	SetBy string `json:"set_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionGoal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessiongoal.FieldSessionUUID, sessiongoal.FieldGoal, sessiongoal.FieldStatus, sessiongoal.FieldTasks, sessiongoal.FieldSetBy:
			values[i] = new(sql.NullString)
		case sessiongoal.FieldCreatedAt, sessiongoal.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case sessiongoal.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionGoal fields.
func (_m *SessionGoal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessiongoal.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case sessiongoal.FieldSessionUUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_uuid", values[i])
			} else if value.Valid {
				_m.SessionUUID = value.String
			}
		case sessiongoal.FieldGoal:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field goal", values[i])
			} else if value.Valid {
				_m.Goal = value.String
			}
		case sessiongoal.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case sessiongoal.FieldTasks:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tasks", values[i])
			} else if value.Valid {
				_m.Tasks = value.String
			}
		case sessiongoal.FieldSetBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field set_by", values[i])
			} else if value.Valid {
				_m.SetBy = value.String
			}
		case sessiongoal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case sessiongoal.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionGoal.
// This includes values selected through modifiers, order, etc.
func (_m *SessionGoal) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this SessionGoal.
// Note that you need to call SessionGoal.Unwrap() before calling this method if this SessionGoal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SessionGoal) Update() *SessionGoalUpdateOne {
	return NewSessionGoalClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SessionGoal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SessionGoal) Unwrap() *SessionGoal {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionGoal is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SessionGoal) String() string {
	var builder strings.Builder
	builder.WriteString("SessionGoal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("session_uuid=")
	builder.WriteString(_m.SessionUUID)
	builder.WriteString(", ")
	builder.WriteString("goal=")
	builder.WriteString(_m.Goal)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("tasks=")
	builder.WriteString(_m.Tasks)
	builder.WriteString(", ")
	builder.WriteString("set_by=")
	builder.WriteString(_m.SetBy)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SessionGoals is a parsable slice of SessionGoal.
type SessionGoals []*SessionGoal
